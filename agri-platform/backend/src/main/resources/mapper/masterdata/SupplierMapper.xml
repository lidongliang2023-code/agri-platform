<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.agri.masterdata.mapper.SupplierMapper">

    <select id="selectSupplierList" resultType="com.agri.masterdata.vo.SupplierVO">
        SELECT s.*, o.org_name AS orgName
        FROM agri_md_supplier s
        LEFT JOIN agri_md_organization o ON s.org_id = o.id
        WHERE s.del_flag = 0
        ORDER BY s.create_time DESC
    </select>

    <select id="selectSupplierById" resultType="com.agri.masterdata.vo.SupplierVO">
        SELECT s.*, o.org_name AS orgName
        FROM agri_md_supplier s
        LEFT JOIN agri_md_organization o ON s.org_id = o.id
        WHERE s.id = #{id} AND s.del_flag = 0
    </select>

    <select id="selectBySupplierCode" resultType="com.agri.masterdata.entity.Supplier">
        SELECT * FROM agri_md_supplier WHERE supplier_code = #{supplierCode} AND del_flag = 0
    </select>
</mapper>
