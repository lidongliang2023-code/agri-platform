<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.agri.masterdata.mapper.MenuMapper">

    <select id="selectMenuTree" resultType="com.agri.masterdata.vo.MenuVO">
        SELECT m.*
        FROM agri_md_menu m
        WHERE m.del_flag = 0 AND m.status = 1
        ORDER BY m.order_num ASC
    </select>

    <select id="selectUserPermissions" resultType="java.lang.String">
        SELECT DISTINCT m.perms
        FROM agri_md_user_role ur
        JOIN agri_md_role_menu rm ON ur.role_id = rm.role_id
        JOIN agri_md_menu m ON rm.menu_id = m.id
        WHERE ur.user_id = #{userId} AND m.perms IS NOT NULL AND m.perms != ''
    </select>

    <select id="selectMenusByRoleId" resultType="com.agri.masterdata.vo.MenuVO">
        SELECT m.*
        FROM agri_md_role_menu rm
        JOIN agri_md_menu m ON rm.menu_id = m.id
        WHERE rm.role_id = #{roleId} AND m.del_flag = 0
        ORDER BY m.order_num ASC
    </select>
</mapper>
