# 农业产业互联网平台·主数据管理模块系统PRD文档

**版本：V1.1**  
**编制日期：2025年1月**  
**文档状态：正式稿**

---

## 文档概述

### 1.1 版本信息

| 属性 | 内容 |
|:-----|:-----|
| 产品名称 | 农业产业互联网平台·主数据管理模块 |
| 文档名称 | 主数据管理模块系统PRD（平台核心基础设施层） |
| 文档版本 | V1.1 |
| 所属平台 | 农业产业互联网平台 |
| 编制部门 | 产品研发中心 |
| 密级 | 内部资料 |

### 1.2 修订记录

| 版本 | 日期 | 修订内容 | 修订人 | 审核人 |
|:-----|:-----|:---------|:-------|:-------|
| V1.0 | 2025年1月 | 初始版本发布，涵盖八大主数据域完整设计 | 产品团队 | - |
| V1.1 | 2025年1月 | 重点补充多租户隔离功能设计；统一API路径(/api/v1/masterdata/)和前端路由(/masterdata/)；扩展RBAC数据权限范围(新增TENANT)；补充租户管理员角色；适配单体架构分发同步机制 | 产品团队 | - |

### 1.3 编写目的

本PRD文档旨在为农业产业互联网平台主数据管理模块的开发实施提供完整的产品需求说明，作为开发团队、设计团队、测试团队及项目管理团队的协作基准文档。

本文档的核心目标包括：

1. **明确功能范围**：详细定义主数据管理模块的八大主数据域、每个域的子功能、页面结构、字段定义、操作流程、交互说明、异常处理和业务规则
2. **统一理解标准**：为所有参与方提供一致的产品理解，避免因认知差异导致的开发偏差
3. **指导开发实施**：提供可直接用于开发的技术规格和业务规则，确保开发团队能够准确实现产品需求
4. **支撑测试验证**：为测试团队提供完整的验收标准，确保产品质量符合预期
5. **协同平台建设**：作为平台核心基础设施层，为其他八大业务模块提供统一的主数据服务能力支撑

### 1.4 读者对象

| 读者角色 | 阅读重点 |
|:---------|:---------|
| 产品经理 | 整体产品架构、功能优先级、产品目标与定位 |
| UI/UX设计师 | 交互设计规范、页面布局、组件规范、响应式设计要求 |
| 前端开发工程师 | 交互逻辑、页面结构、接口规范、技术栈要求 |
| 后端开发工程师 | 业务规则、接口设计、数据存储、非功能性需求 |
| 测试工程师 | 功能详细设计、验收标准、数据埋点、异常处理 |
| 项目经理 | 功能清单、实施计划、资源评估 |
| 运营人员 | 用户使用场景、产品价值、数据分析需求 |
| 业务模块开发团队 | 主数据API调用、数据同步机制、数据权限配置 |

### 1.5 术语定义

| 术语 | 英文 | 定义 |
|:-----|:-----|:-----|
| 主数据 | Master Data | 在企业内跨越多个业务部门、被多个业务用户共同使用的核心业务数据 |
| 主数据管理 | Master Data Management | 管理主数据的标准定义、创建、分发、治理的完整体系 |
| 用户主数据 | User Master Data | 平台所有用户实体的统一档案，涵盖身份、认证、画像等信息 |
| 组织主数据 | Organization Master Data | 平台各类组织机构的信息，包括企业、合作社、家庭农场等 |
| 权限主数据 | Permission Master Data | 基于RBAC模型的权限配置数据，包括角色、权限、资源等 |
| 商品主数据 | Product Master Data | 农产品的标准化信息，包括分类、属性、规格、编码等 |
| 客户供应商主数据 | Customer Supplier Master Data | 交易双方的完整档案和关系管理 |
| 地址主数据 | Address Master Data | 行政区划、产区、物流节点等地理信息 |
| 数据标准管理 | Data Standard Management | 数据字段、编码、命名等规范的定义与管理 |
| 数据质量管理 | Data Quality Management | 数据质量规则、检测、问题处理的完整体系 |
| RBAC | Role-Based Access Control | 基于角色的访问控制，一种权限管理模型 |
| SPU | Standard Product Unit | 标准化产品单元，商品信息聚合的最小单位 |
| SKU | Stock Keeping Unit | 库存量单位，商品最小库存单位 |
| MDG | Master Data Governance | 主数据治理，确保主数据质量和使用规范的管理活动 |

### 1.6 参考文档

| 文档名称 | 版本 | 关联说明 |
|:---------|:-----|:---------|
| 农业产业互联网平台·主数据模块深化设计方案 | V1.0 | 本PRD的设计依据文档，提供详细的功能设计和技术方案 |
| 农业产业互联网平台·总体产品设计方案 | - | 平台级产品设计文档，定义模块间协同关系 |
| 农业产业互联网平台·交易撮合模块系统PRD | V1.0 | PRD格式参考文档 |
| 农业产业互联网平台·技术架构设计方案 | - | 平台级技术架构定义 |
| 《个人信息保护法》 | - | 个人信息处理的合规依据 |
| 《数据安全法》 | - | 数据安全管理的合规依据 |
| 《网络安全法》 | - | 网络安全的合规依据 |

---

## 产品定位与目标

### 2.1 产品定位

主数据管理模块是农业产业互联网平台的**核心基础设施层**，位于平台架构的底座位置，为所有业务模块提供统一的主数据服务能力。作为平台的"数据根基"，主数据模块承载着用户身份管理、组织架构管理、权限控制管理、商品标准管理、客户供应商管理等核心数据资产，是确保平台数据一致性、支撑业务协同的关键保障。

```
┌─────────────────────────────────────────────────────────────────────────────────┐
│                           农业产业互联网平台架构                                   │
├─────────────────────────────────────────────────────────────────────────────────┤
│                                                                                 │
│  ┌─────────────────────────────────────────────────────────────────────────┐     │
│  │                          应用层 (Application Layer)                       │     │
│  │                                                                         │     │
│  │    ┌─────────────┐  ┌─────────────┐  ┌─────────────┐                  │     │
│  │    │  生产管理   │  │  交易撮合   │  │   S2B2C     │                  │     │
│  │    └─────────────┘  └─────────────┘  └─────────────┘                  │     │
│  │    ┌─────────────┐  ┌─────────────┐  ┌─────────────┐                  │     │
│  │    │ 供应链金融  │  │  信息资讯   │  │  综合服务   │                  │     │
│  │    └─────────────┘  └─────────────┘  └─────────────┘                  │     │
│  │    ┌─────────────────────────────────────────────────────────────────┐ │     │
│  │    │                       政府监管模块                               │ │     │
│  │    └─────────────────────────────────────────────────────────────────┘ │     │
│  └─────────────────────────────────────────────────────────────────────────┘     │
│                                         │                                        │
│                                         ▼                                        │
│  ┌─────────────────────────────────────────────────────────────────────────┐     │
│  │                      业务中台层 (Business Middle Platform)               │     │
│  │                                                                         │     │
│  │    ┌─────────────────────────────────────────────────────────────────┐ │     │
│  │    │                     主数据管理模块 ⭐ (核心基础设施)               │ │     │
│  │    │                                                                 │ │     │
│  │    │  ┌───────────┐ ┌───────────┐ ┌───────────┐ ┌───────────┐       │ │     │
│  │    │  │用户主数据 │ │组织主数据 │ │权限主数据 │ │商品主数据 │       │ │     │
│  │    │  └───────────┘ └───────────┘ └───────────┘ └───────────┘       │ │     │
│  │    │  ┌───────────┐ ┌───────────┐ ┌───────────┐ ┌───────────┐       │ │     │
│  │    │  │客户供应商 │ │地址主数据 │ │数据标准   │ │数据质量   │       │ │     │
│  │    │  └───────────┘ └───────────┘ └───────────┘ └───────────┘       │ │     │
│  │    └─────────────────────────────────────────────────────────────────┘ │     │
│  │                                                                         │     │
│  │    ┌─────────────┐  ┌─────────────┐  ┌─────────────┐                  │     │
│  │    │  订单服务   │  │  支付服务   │  │  物流服务   │                  │     │
│  │    └─────────────┘  └─────────────┘  └─────────────┘                  │     │
│  └─────────────────────────────────────────────────────────────────────────┘     │
│                                         │                                        │
│                                         ▼                                        │
│  ┌─────────────────────────────────────────────────────────────────────────┐     │
│  │                          数据中台层 (Data Middle Platform)               │     │
│  │                                                                         │     │
│  │    ┌─────────────┐  ┌─────────────┐  ┌─────────────┐                  │     │
│  │    │  数据采集   │  │  数据治理   │  │  数据服务   │                  │     │
│  │    └─────────────┘  └─────────────┘  └─────────────┘                  │     │
│  └─────────────────────────────────────────────────────────────────────────┘     │
│                                         │                                        │
│                                         ▼                                        │
│  ┌─────────────────────────────────────────────────────────────────────────┐     │
│  │                      基础设施层 (Infrastructure Layer)                   │     │
│  │    ┌─────────────┐  ┌─────────────┐  ┌─────────────┐  ┌─────────────┐  │     │
│  │    │  云服务器   │  │  数据库     │  │  缓存服务   │  │  消息队列   │  │     │
│  │    └─────────────┘  └─────────────┘  └─────────────┘  └─────────────┘  │     │
│  └─────────────────────────────────────────────────────────────────────────┘     │
│                                                                                 │
└─────────────────────────────────────────────────────────────────────────────────┘
```

### 2.2 核心价值主张

主数据管理模块的核心价值主张凝练为三个维度：**数据一致性、数据质量、数据治理**。

#### 2.2.1 数据一致性价值

主数据管理通过建立统一的数据标准、编码规则和同步机制，确保同一数据在平台各模块、各系统中保持一致。例如，农户"张三"在用户主数据中只有一个唯一标识，在生产管理、交易撮合、金融服务等各模块中均引用该标识，避免因数据重复、冲突导致业务混乱。

**用户价值**：
- 业务系统：统一数据来源，避免数据孤岛
- 运营人员：数据口径一致，统计分析准确
- 业务协同：跨模块数据共享顺畅

#### 2.2.2 数据质量价值

主数据管理通过数据质量规则定义、质量检测和问题管理，持续提升平台数据资产的可用性。高质量的主数据是智能匹配、信用评估、风控模型等数据应用的基础，直接影响平台的服务能力和用户体验。

**用户价值**：
- 业务系统：数据准确可靠，业务决策有依据
- 平台运营：质量指标可视化管理
- 客户体验：信息准确无误，服务体验提升

#### 2.2.3 数据治理价值

主数据管理建立完善的数据治理体系，包括数据标准管理、数据安全管理、数据资产管理等，为平台的数据化运营提供制度保障和组织支撑。主数据治理能力是平台长期竞争力的重要组成部分。

**用户价值**：
- 合规要求：满足个保法、数据安全法等合规要求
- 安全管理：敏感数据加密，访问审计追溯
- 资产管理：数据资产清晰可视，价值可评估

### 2.3 产品目标

#### 2.3.1 能力目标

| 目标维度 | 目标描述 |
|:---------|:---------|
| 统一数据服务 | 为8大业务模块提供统一、标准、一致的主数据服务 |
| 数据标准化 | 建立覆盖用户、商品、组织、地址等领域的编码规范和标准体系 |
| 质量管控 | 实现主数据全生命周期的质量监控和治理 |
| 权限安全 | 建立精细化的RBAC权限体系，确保数据访问安全 |
| 分发同步 | 建立高效、稳定的主数据分发同步机制，保障数据一致性 |

#### 2.3.2 成功指标

| 指标维度 | 指标名称 | 目标值 |
|:---------|:---------|:-------|
| 数据一致性 | 主数据分发成功率 | >99.5% |
| 数据一致性 | 跨模块数据差异率 | <0.1% |
| 数据质量 | 主数据完整率 | >98% |
| 数据质量 | 主数据准确率 | >99% |
| 性能指标 | 主数据查询响应时间(P99) | <200ms |
| 性能指标 | 批量同步延迟 | <5s |
| 安全性 | 敏感数据加密覆盖率 | 100% |
| 安全性 | 权限审计覆盖率 | 100% |

---

## 多租户架构设计

### X.1 多租户架构概述

#### X.1.1 架构定位

多租户架构是主数据管理模块的核心基础设施能力，为平台提供多租户数据隔离能力。本模块采用**共享数据库+共享Schema+行级租户隔离**的架构模式（MVP阶段推荐方案），在保证数据隔离的同时，最大限度降低运维成本和技术复杂度。

**选择该方案的理由**：
- **成本最优**：无需为每个租户创建独立数据库，数据库资源成本最低
- **运维最简**：单一数据库实例，备份、迁移、监控均统一管理
- **扩展性强**：通过水平扩展数据库即可支撑租户数量增长
- **符合阶段**：MVP阶段业务模型尚未稳定，行级隔离便于租户数据迁移合并

**架构示意图**：

```
┌─────────────────────────────────────────────────────────────────────────────────┐
│                           多租户隔离架构                                          │
├─────────────────────────────────────────────────────────────────────────────────┤
│                                                                                 │
│  ┌─────────────────────────────────────────────────────────────────────────┐     │
│  │                          共享数据库实例                                  │     │
│  │                                                                         │     │
│  │  ┌─────────────────────────────────────────────────────────────────┐   │     │
│  │  │                        共享 Schema                                │   │     │
│  │  │                                                                  │   │     │
│  │  │   ┌─────────────┐   ┌─────────────┐   ┌─────────────┐         │   │     │
│  │  │   │ tenant_id=1 │   │ tenant_id=2 │   │ tenant_id=3 │  ...   │   │     │
│  │  │   │   租户A数据   │   │   租户B数据   │   │   租户C数据   │         │   │     │
│  │  │   └─────────────┘   └─────────────┘   └─────────────┘         │   │     │
│  │  │                                                                  │   │     │
│  │  └─────────────────────────────────────────────────────────────────┘   │     │
│  │                                                                         │     │
│  └─────────────────────────────────────────────────────────────────────────┘     │
│                                                                                 │
│  ┌─────────────────────────────────────────────────────────────────────────┐     │
│  │                          租户隔离层                                      │     │
│  │                                                                          │     │
│  │   MyBatis拦截器 ──► 自动注入tenant_id ──► 查询条件自动拼接              │     │
│  │   Service层 ──► ThreadLocal租户上下文 ──► 自动传递                     │     │
│  │                                                                          │     │
│  └─────────────────────────────────────────────────────────────────────────┘     │
│                                                                                 │
└─────────────────────────────────────────────────────────────────────────────────┘
```

#### X.1.2 租户类型定义

| 租户类型 | 类型代码 | 说明 | 典型用户 |
|:---------|:---------|:-----|:---------|
| 平台级租户 | platform | 平台超级管理员，可管理所有租户和全局配置 | 平台运营方 |
| 企业级租户 | enterprise | 独立组织架构和用户体系，有完整的业务管理能力 | 农业企业、合作社、农业园区 |
| 个人级租户 | personal | 数据完全隔离，适合小B商家或个体农户 | 个体农户、小B商家、农资经销商 |

#### X.1.3 BaseEntity基座设计

所有业务表继承BaseEntity基类，确保tenant_id字段统一管理：

```java
public abstract class BaseEntity {
    // 租户ID，所有业务表必须字段
    private String tenantId;
    
    // 其他通用字段
    private String createBy;
    private LocalDateTime createTime;
    private String updateBy;
    private LocalDateTime updateTime;
}
```

### X.2 租户生命周期管理

#### X.2.1 租户状态流转

```
┌─────────┐    创建     ┌─────────┐    审核通过   ┌─────────┐    启用      ┌─────────┐
│   草稿   │ ────────► │  待审核  │ ──────────► │  待配置  │ ────────► │  正常   │
└─────────┘            └─────────┘              └─────────┘            └─────────┘
     │                     │                       │                     │
     │                     │ 审核拒绝               │                     │
     │                     ▼                       │                     ▼
     │                ┌─────────┐                 │                ┌─────────┐
     │                │  已拒绝  │                 │                │  停用   │
     │                └─────────┘                 │                └─────────┘
     │                                            │                     │
     │                                            │                     ▼
     │                                            │                ┌─────────┐
     │                                            │                │  归档   │
     │                                            │                └─────────┘
     │                                            │
     │                                            ▼
     └──────────────────────────────────────►  ┌─────────┐
                                                 │  欠费   │
                                                 └─────────┘
```

#### X.2.2 租户创建流程

1. **注册申请**：填写租户基本信息（名称、类型、联系人等）
2. **资料审核**：平台管理员审核资质
3. **配置初始化**：创建租户管理员、初始化套餐配额
4. **服务开通**：激活租户账号，分配初始存储空间
5. **完成入驻**：租户可正常登录使用

#### X.2.3 租户注销流程

1. **申请注销**：租户管理员提交注销申请
2. **数据备份**：系统自动备份租户数据
3. **欠款清算**：结清所有费用
4. **审批确认**：平台管理员审批确认
5. **数据归档**：数据转入归档状态，保留30天后彻底删除

### X.3 租户隔离维度设计

#### X.3.1 隔离策略总览

| 隔离维度 | 隔离方式 | 说明 |
|:---------|:---------|:-----|
| 用户数据 | tenant_id行级隔离 | 每个租户只能看到自己的用户 |
| 组织数据 | tenant_id行级隔离 | 组织架构租户内独立 |
| 角色数据 | 混合隔离 | 系统预置角色共享，自定义角色租户隔离 |
| 权限数据 | 共享 | 权限树全平台统一 |
| 商品主数据 | 混合隔离 | 平台标准分类共享，租户自定义分类隔离 |
| 数据字典 | 混合隔离 | 系统字典共享，租户字典隔离 |
| 地址/行政区划 | 共享 | 全平台共享，国标数据 |
| 操作日志 | tenant_id行级隔离 | 审计日志租户隔离 |

#### X.3.2 详细隔离说明

**1. 用户主数据（完全隔离）**

```
租户A: user_master (tenant_id='A')
  ├── 用户A1
  ├── 用户A2
  └── 用户A3

租户B: user_master (tenant_id='B')
  ├── 用户B1
  └── 用户B2
```

**2. 权限主数据（部分共享）**

```
系统预置角色（共享，platform级管理）:
  ├── SUPER_ADMIN（超级管理员）
  ├── PLATFORM_ADMIN（平台管理员）
  └── TENANT_ADMIN（租户管理员）

租户自定义角色（租户隔离）:
  租户A: 合作社社长、业务员
  租户B: 批发商管理员、采购员
```

**3. 商品分类（混合隔离）**

```
平台标准分类（共享）:
  ├── 农产品
  │   ├── 粮食作物（稻谷、小麦、玉米）
  │   ├── 蔬菜（叶菜类、瓜果类）
  │   └── 水果（仁果类、核果类）

租户自定义分类（租户隔离）:
  租户A: 自定义分类A1
  租户B: 自定义分类B1
```

**4. 行政区划（完全共享）**

```
平台共享表: address_region
  ├── 110000 北京市
  │   ├── 110100 北京市辖区
  │   └── ...
  └── ...
所有租户共享使用国标行政区划数据
```

### X.4 技术实现方案

#### X.4.1 租户上下文传递机制

```
┌─────────────────────────────────────────────────────────────────────────────────┐
│                          租户上下文传递流程                                       │
├─────────────────────────────────────────────────────────────────────────────────┤
│                                                                                 │
│  用户请求 ──► Filter解析Token ──► 获取tenant_id ──► 存入ThreadLocal            │
│                                                                                 │
│                                    │                                            │
│                                    ▼                                            │
│                           ┌────────────────┐                                    │
│                           │   ThreadLocal  │                                    │
│                           │  TenantContext │                                    │
│                           │  {tenantId:xxx} │                                   │
│                           └────────────────┘                                    │
│                                    │                                            │
│        ┌──────────────────────────┼──────────────────────────┐                │
│        │                          │                          │                │
│        ▼                          ▼                          ▼                │
│  ┌───────────┐            ┌───────────┐            ┌───────────┐              │
│  │ Controller │            │  Service  │            │    DAO    │              │
│  │           │            │           │            │           │              │
│  │ 读取上下文 │ ────────► │ 传递上下文 │ ────────► │ 自动注入   │              │
│  │ 业务校验  │            │ 业务处理   │            │ tenant_id │              │
│  └───────────┘            └───────────┘            └───────────┘              │
│                                                                                 │
│  MyBatis拦截器自动拼接: WHERE tenant_id = #{tenantId}                          │
│                                                                                 │
└─────────────────────────────────────────────────────────────────────────────────┘
```

#### X.4.2 MyBatis租户拦截器实现

```java
@Intercepts({
    @Signature(type = StatementHandler.class, method = "prepare", 
               args = {Connection.class, Integer.class})
})
public class TenantInterceptor implements Interceptor {
    
    @Override
    public Object intercept(Invocation invocation) throws Throwable {
        // 1. 获取当前租户ID
        String tenantId = TenantContextHolder.getTenantId();
        
        // 2. 获取SQL语句
        String sql = statementHandler.getBoundSql().getSql();
        
        // 3. 判断是否需要注入租户条件（排除系统表）
        if (shouldInjectTenant(sql)) {
            // 4. 注入tenant_id条件
            sql = injectTenantCondition(sql, tenantId);
            // 5. 更新SQL
            BoundSql boundSql = statementHandler.getBoundSql();
            MetaObject metaObject = SystemMetaObject.forObject(boundSql);
            metaObject.setValue("sql", sql);
        }
        
        return invocation.proceed();
    }
    
    /**
     * 判断是否需要注入租户条件
     * 系统表（tenant_info、tenant_config等）不需要注入
     */
    private boolean shouldInjectTenant(String sql) {
        // 排除系统表和白名单表
        return !isSystemTable(sql);
    }
}
```

#### X.4.3 租户穿透查询（超级管理员）

超级管理员可切换租户视角进行穿透查询：

```java
// 超级管理员设置穿透租户
TenantContextHolder.set穿透Mode(true);
TenantContextHolder.set穿透TenantId("租户A");

// 查询租户A的数据
List<User> users = userService.listUsers();

// 清除穿透模式
TenantContextHolder.clear穿透Mode();
```

**前端实现**：在平台管理后台提供租户切换下拉框，超级管理员可切换查看不同租户的数据。

### X.5 租户配额管理

#### X.5.1 配额项定义

| 配额项 | 说明 | 默认配额（基础版） | 默认配额（专业版） |
|:-------|:-----|:------------------|:------------------|
| max_users | 最大用户数 | 50 | 500 |
| max_storage | 最大存储空间(GB) | 10 | 100 |
| max_organizations | 最大组织数 | 5 | 50 |
| max_products | 最大商品数 | 100 | 2000 |
| max_api_calls | 每日API调用次数 | 10000 | 100000 |
| max_custom_fields | 最大自定义字段数 | 10 | 50 |

#### X.5.2 配额检查机制

```java
// 在创建资源前检查配额
public void checkQuota(String tenantId, String resourceType, int count) {
    TenantConfig config = tenantConfigService.getConfig(tenantId);
    int currentCount = resourceService.countByTenant(tenantId, resourceType);
    int maxCount = config.getQuota(resourceType);
    
    if (currentCount + count > maxCount) {
        throw new QuotaExceededException(
            "租户[{}]的[{}]配额已用完，当前:{}/{}",
            tenantId, resourceType, currentCount, maxCount
        );
    }
}
```

#### X.5.3 配额预警

- **80%预警**：配额使用达到80%时发送邮件通知租户管理员
- **90%预警**：配额使用达到90%时发送短信通知
- **100%拦截**：配额用完时阻止创建新资源，提示升级套餐

### X.6 租户数据导出与迁移

#### X.6.1 数据导出

租户可导出本租户内的所有数据：

- **导出格式**：JSON、Excel、CSV
- **导出内容**：用户数据、组织数据、商品数据、交易数据等
- **导出权限**：仅租户管理员可操作
- **审计记录**：导出操作记入审计日志

#### X.6.2 租户数据迁移接口（V2.0）

预留租户数据迁移接口，用于未来租户独立部署场景：

```java
/**
 * 租户数据导出接口
 * @param tenantId 租户ID
 * @param exportFormat 导出格式
 * @return 导出文件路径
 */
String exportTenantData(String tenantId, String exportFormat);

/**
 * 租户数据导入接口（跨租户迁移）
 * @param targetTenantId 目标租户ID
 * @param dataFile 数据文件
 */
void importTenantData(String targetTenantId, MultipartFile dataFile);
```

### X.7 新增租户管理功能

#### X.7.1 页面清单

| 页面名称 | 路径 | 主要功能 | 用户角色 |
|:---------|:-----|:---------|:---------|
| 租户列表 | /masterdata/tenant/list | 租户查询、状态管理 | 平台管理员 |
| 租户详情 | /masterdata/tenant/detail/{id} | 租户信息查看、编辑 | 平台管理员 |
| 租户配置 | /masterdata/tenant/config/{id} | 配额配置、功能开关 | 平台管理员 |
| 租户套餐 | /masterdata/tenant/package | 套餐定义、定价管理 | 平台管理员 |
| 租户统计 | /masterdata/tenant/statistics | 租户数据分析 | 平台管理员 |

#### X.7.2 核心API接口

| 接口名称 | 请求方式 | 路径 | 说明 |
|:---------|:---------|:-----|:-----|
| 租户列表 | GET | /api/v1/masterdata/tenants | 分页查询租户 |
| 租户详情 | GET | /api/v1/masterdata/tenants/{id} | 获取租户详情 |
| 租户创建 | POST | /api/v1/masterdata/tenants | 创建新租户 |
| 租户更新 | PUT | /api/v1/masterdata/tenants/{id} | 更新租户信息 |
| 租户启用/停用 | PUT | /api/v1/masterdata/tenants/{id}/status | 修改租户状态 |
| 租户配额查询 | GET | /api/v1/masterdata/tenants/{id}/quota | 获取租户配额 |
| 租户配额配置 | PUT | /api/v1/masterdata/tenants/{id}/quota | 配置租户配额 |
| 租户数据统计 | GET | /api/v1/masterdata/tenants/{id}/statistics | 获取统计数据 |
| 租户穿透查询 | GET | /api/v1/masterdata/tenants/{id}/query | 穿透查询租户数据 |

### X.8 多租户安全设计

#### X.8.1 数据隔离安全保障

1. **强制租户校验**：所有业务表操作必须携带tenant_id
2. **越权访问拦截**：拦截器校验当前用户所属租户与数据租户是否匹配
3. **跨租户操作禁止**：禁止任何跨租户的数据访问和操作
4. **审计追溯**：所有操作记录租户ID，便于事后审计

#### X.8.2 敏感操作二次验证

| 操作类型 | 验证方式 | 说明 |
|:---------|:---------|:-----|
| 租户删除 | 短信验证码 + 密码确认 | 防止误操作 |
| 租户迁移 | 管理员审批 | 需上级管理员确认 |
| 配额修改 | 操作日志记录 | 所有变更可追溯 |
| 数据导出 | 密码确认 | 防止数据泄露 |

---

## 用户画像与使用场景

### 3.1 目标用户画像

#### 3.1.1 平台管理员

**基本信息**：

| 属性 | 描述 |
|:-----|:-----|
| 典型画像 | 30-45岁，IT背景，负责平台整体运维 |
| 设备偏好 | PC端为主，需要大屏监控 |
| 教育背景 | 本科及以上，计算机或信息管理专业 |

**核心诉求**：
- 平台整体数据可视化管理
- 系统配置与权限管理
- 跨模块数据协调

**用户故事**：

> As a 平台管理员，我想要**统一管理所有主数据域**，so that **我能确保平台数据的一致性和质量**。

> As a 平台管理员，我想要**配置数据分发规则**，so that **主数据变更能自动同步到各业务模块**。

#### 3.1.2 数据管理员

**基本信息**：

| 属性 | 描述 |
|:-----|:-----|
| 典型画像 | 25-40岁，数据管理背景，负责主数据日常运维 |
| 设备偏好 | PC端，需要处理大量数据表格 |
| 教育背景 | 本科及以上，数据分析或相关专业 |

**核心诉求**：
- 主数据日常维护与审核
- 数据质量监控与问题处理
- 数据标准制定与推广

**用户故事**：

> As a 数据管理员，我想要**审核新用户注册申请**，so that **平台用户身份真实可靠**。

> As a 数据管理员，我想要**管理商品分类标准**，so that **商品信息规范统一**。

#### 3.1.3 业务模块管理员

**基本信息**：

| 属性 | 描述 |
|:-----|:-----|
| 典型画像 | 25-35岁，业务背景，负责各业务模块的主数据需求 |
| 设备偏好 | PC端为主，移动端辅助 |
| 教育背景 | 大专及以上 |

**核心诉求**：
- 主数据查询与订阅
- 业务相关主数据配置
- 数据质量反馈

**用户故事**：

> As a 业务模块管理员，我想要**查询商品主数据**，so that **我能快速获取标准化的商品信息**。

> As a 业务模块管理员，我想要**订阅主数据变更事件**，so that **我的业务系统能实时同步最新数据**。

#### 3.1.4 开发者

**基本信息**：

| 属性 | 描述 |
|:-----|:-----|
| 典型画像 | 25-35岁，技术背景，需要调用主数据API |
| 设备偏好 | PC端，需要API调试工具 |
| 教育背景 | 本科及以上，计算机专业 |

**核心诉求**：
- 完整的API文档
- 稳定可靠的接口服务
- 数据权限控制

**用户故事**：

> As a 开发者，我想要**调用用户主数据API**，so that **我的业务模块能获取用户身份信息**。

> As a 开发者，我想要**订阅商品分类变更事件**，so that **我的商品列表能自动更新**。

---

## 功能架构

### 4.1 功能架构总览

```
┌─────────────────────────────────────────────────────────────────────────────────┐
│                          主数据管理模块功能架构                                   │
├─────────────────────────────────────────────────────────────────────────────────┤
│                                                                                 │
│  ┌─────────────────────────────────────────────────────────────────────────────┐ │
│  │                          八大主数据域                                        │ │
│  ├─────────────────────────────────────────────────────────────────────────────┤ │
│  │                                                                             │ │
│  │  ┌───────────────────────┐  ┌───────────────────────┐                       │ │
│  │  │    4.1 用户主数据       │  │    4.2 组织主数据      │                       │ │
│  │  │                       │  │                       │                       │ │
│  │  │  • 用户列表/详情      │  │  • 组织架构管理       │                       │ │
│  │  │  • 身份管理           │  │  • 部门/岗位管理       │                       │ │
│  │  │  • 用户画像           │  │  • 组织变更管理       │                       │ │
│  │  │  • 实名认证           │  │  • 组织详情           │                       │ │
│  │  │  • 用户合并/拆分      │  │                       │                       │ │
│  │  └───────────────────────┘  └───────────────────────┘                       │ │
│  │                                                                             │ │
│  │  ┌───────────────────────┐  ┌───────────────────────┐                       │ │
│  │  │    4.3 权限主数据      │  │    4.4 商品主数据      │                       │ │
│  │  │                       │  │                       │                       │ │
│  │  │  • 角色管理           │  │  • 分类管理           │                       │ │
│  │  │  • 权限资源管理       │  │  • 标准库管理         │                       │ │
│  │  │  • 数据权限配置       │  │  • 审核管理           │                       │ │
│  │  │  • 权限审计日志       │  │  • 编码规则           │                       │ │
│  │  └───────────────────────┘  └───────────────────────┘                       │ │
│  │                                                                             │ │
│  │  ┌───────────────────────┐  ┌───────────────────────┐                       │ │
│  │  │ 4.5 客户供应商主数据    │  │  4.6 地址主数据       │                       │ │
│  │  │                       │  │                       │                       │ │
│  │  │  • 客户档案管理       │  │  • 行政区划管理       │                       │ │
│  │  │  • 供应商档案管理     │  │  • 地址标准化         │                       │ │
│  │  │  • 关系管理           │  │  • 产区管理           │                       │ │
│  │  │                       │  │  • 物流节点管理       │                       │ │
│  │  └───────────────────────┘  └───────────────────────┘                       │ │
│  │                                                                             │ │
│  │  ┌───────────────────────┐  ┌───────────────────────┐                       │ │
│  │  │   4.7 数据标准管理     │  │   4.8 数据质量管理    │                       │ │
│  │  │                       │  │                       │                       │ │
│  │  │  • 标准定义           │  │  • 质量规则配置       │                       │ │
│  │  │  • 标准审核发布       │  │  • 检测任务管理       │                       │ │
│  │  │  • 标准对标检测       │  │  • 问题工单           │                       │ │
│  │  │  • 数据字典           │  │  • 质量仪表盘         │                       │ │
│  │  └───────────────────────┘  └───────────────────────┘                       │ │
│  │                                                                             │ │
│  └─────────────────────────────────────────────────────────────────────────────┘ │
│                                                                                 │
│  ┌─────────────────────────────────────────────────────────────────────────────┐ │
│  │                          基础能力层                                         │ │
│  ├─────────────────────────────────────────────────────────────────────────────┤ │
│  │                                                                             │ │
│  │  ┌─────────────┐  ┌─────────────┐  ┌─────────────┐  ┌─────────────┐       │ │
│  │  │  数据分发   │  │  运营管理   │  │  区块链增强 │  │  数据安全   │       │ │
│  │  │  与同步     │  │  后台      │  │            │  │            │       │ │
│  │  └─────────────┘  └─────────────┘  └─────────────┘  └─────────────┘       │ │
│  │                                                                             │ │
│  └─────────────────────────────────────────────────────────────────────────────┘ │
│                                                                                 │
└─────────────────────────────────────────────────────────────────────────────────┘
```

### 4.2 各域功能清单

| 主数据域 | 一级功能 | 二级功能 | 优先级 |
|:---------|:---------|:---------|:-------|
| 用户主数据 | 用户管理 | 用户列表、用户详情、用户创建/编辑、用户合并/拆分 | P0 |
| 用户主数据 | 身份管理 | 身份配置、身份切换、身份认证 | P0 |
| 用户主数据 | 画像管理 | 画像标签、画像数据展示 | P1 |
| 用户主数据 | 认证管理 | 认证审核、证照管理 | P0 |
| 组织主数据 | 组织架构 | 组织树、部门管理、岗位管理 | P0 |
| 组织主数据 | 组织详情 | 基本信息、成员列表、关联业务 | P0 |
| 组织主数据 | 变更管理 | 组织合并、拆分、层级调整 | P1 |
| 权限主数据 | 角色管理 | 角色定义、权限配置、角色成员 | P0 |
| 权限主数据 | 权限资源 | 菜单权限、功能权限、API权限 | P0 |
| 权限主数据 | 数据权限 | 数据隔离规则配置 | P0 |
| 权限主数据 | 审计日志 | 权限变更记录、操作日志 | P1 |
| 商品主数据 | 分类管理 | 分类树、分类属性、分类关联 | P0 |
| 商品主数据 | 标准库 | SPU管理、规格定义、属性模板 | P0 |
| 商品主数据 | 审核管理 | 商品审核流程、审核记录 | P0 |
| 商品主数据 | 编码规则 | SKU/SPU编码规则配置 | P0 |
| 客户供应商主数据 | 客户档案 | 客户列表、客户详情、客户分类 | P0 |
| 客户供应商主数据 | 供应商档案 | 供应商列表、资质管理、供应品类 | P0 |
| 客户供应商主数据 | 关系管理 | 关联关系、交易历史 | P1 |
| 地址主数据 | 行政区划 | 省市区三级联动、编码管理 | P0 |
| 地址主数据 | 地址标准化 | 地址清洗、地址补全、地址验证 | P1 |
| 数据标准管理 | 标准定义 | 字段标准、编码标准、命名规范 | P1 |
| 数据标准管理 | 标准审核 | 审核流程、版本管理 | P1 |
| 数据标准管理 | 对标检测 | 一致性检测、问题报告 | P2 |
| 数据质量管理 | 规则配置 | 完整性、准确性、一致性、时效性规则 | P1 |
| 数据质量管理 | 检测任务 | 任务调度、结果记录 | P1 |
| 数据质量管理 | 问题工单 | 问题发现→分析→整改→验证闭环 | P2 |
| 数据质量管理 | 质量仪表盘 | 质量评分、趋势图、问题分布 | P2 |

### 4.3 功能优先级矩阵

| 优先级 | 说明 | 功能范围 |
|:-------|:-----|:---------|
| P0 | 核心功能，必须MVP完成 | 用户管理、身份管理、组织架构、角色权限、商品分类、商品标准库、客户供应商管理、行政区划 |
| P1 | 重要功能，V1.0完成 | 画像管理、组织变更、审计日志、编码规则、关系管理、地址标准化、数据标准管理、数据质量管理 |
| P2 | 增强功能，V2.0完成 | 对标检测、问题工单、质量仪表盘 |

---

## 与平台各业务模块的协同关系

### 5.1 协同关系总览

主数据模块与平台各业务模块形成紧密的支撑与被支撑关系，是各模块正常运行的基础保障。

| 业务模块 | 依赖的主数据 | 依赖说明 |
|:---------|:-------------|:---------|
| **生产管理** | 用户主数据、组织主数据、商品主数据、地址主数据 | 农场档案绑定用户和组织，作物品种引用商品主数据，产地关联行政区划 |
| **交易撮合** | 用户主数据、商品主数据、客户供应商主数据、地址主数据 | 买卖双方身份认证，商品标准化管理，供需双方档案管理 |
| **S2B2C** | 用户主数据、组织主数据、商品主数据、权限主数据 | 小B商家认证管理，商品供应链管理，分销权限控制 |
| **供应链金融** | 用户主数据、组织主数据、客户供应商主数据、数据标准 | 融资主体身份核验，企业资质评估，数据格式标准化 |
| **信息资讯** | 用户主数据、组织主数据、数据标准 | 内容发布者身份管理，数据指标标准化 |
| **综合服务** | 用户主数据、组织主数据、商品主数据、地址主数据 | 服务商认证管理，服务商品标准化，服务区域管理 |
| **政府监管** | 所有主数据 | 监管数据汇总，数据追溯查询，跨系统数据关联 |

### 5.2 详细协同关系

#### 5.2.1 与生产管理模块的协同

| 协同方向 | 数据流向 | 协同内容 | 接口方式 |
|:---------|:---------|:---------|:---------|
| 主数据→生产 | 数据引用 | 用户身份→农场绑定；组织架构→合作社管理；行政区划→农场位置 | API订阅 |
| 生产→主数据 | 数据反馈 | 用户画像丰富；农场认证状态更新；农户信用评分 | API推送 |

#### 5.2.2 与交易撮合模块的协同

| 协同方向 | 数据流向 | 协同内容 | 接口方式 |
|:---------|:---------|:---------|:---------|
| 主数据→交易 | 数据引用 | 用户身份→交易主体认证；商品主数据→商品发布；地址主数据→产地销地 | API订阅 |
| 交易→主数据 | 数据反馈 | 客户供应商主数据更新；交易信用评分更新 | API推送 |

#### 5.2.3 与供应链金融模块的协同

| 协同方向 | 数据流向 | 协同内容 | 接口方式 |
|:---------|:---------|:---------|:---------|
| 主数据→金融 | 数据引用 | 用户身份→融资主体认证；组织认证→企业资质；信用评分→风控基础 | API订阅 |
| 金融→主数据 | 数据反馈 | 信用评估结果更新；融资记录→用户画像 | API推送 |

#### 5.2.4 与政府监管模块的协同

| 协同方向 | 数据流向 | 协同内容 | 接口方式 |
|:---------|:---------|:---------|:---------|
| 主数据→监管 | 数据支持 | 统一用户编码→跨模块追溯；组织架构→市场主体管理；数据标准→数据汇总 | 数据推送 |
| 监管→主数据 | 数据反馈 | 监管评级→用户信用；合规认证→资质更新 | API推送 |

---

## 主数据分发与同步机制

### 6.1 分发架构设计

主数据分发采用事件驱动+定时同步双模式，确保各业务模块及时获取最新的主数据变更。

```
┌─────────────────────────────────────────────────────────────────────────────────┐
│                          主数据分发同步架构（V1.0单体版）                          │
├─────────────────────────────────────────────────────────────────────────────────┤
│                                                                                 │
│  ┌─────────────────────────────────────────────────────────────────────────┐     │
│  │                           主数据服务层                                    │     │
│  │                                                                         │     │
│  │    ┌─────────┐  ┌─────────┐  ┌─────────┐  ┌─────────┐  ┌─────────┐     │     │
│  │    │用户主数据│  │组织主数据│  │权限主数据│  │商品主数据│  │客户供应商│     │     │
│  │    └────┬────┘  └────┬────┘  └────┬────┘  └────┬────┘  └────┬────┘     │     │
│  │         │             │             │             │             │          │     │
│  │         └─────────────┴─────────────┴─────────────┴─────────────┘          │     │
│  │                                    │                                        │     │
│  └────────────────────────────────────┼────────────────────────────────────────┘     │
│                                       │                                              │
│                                       ▼                                              │
│  ┌─────────────────────────────────────────────────────────────────────────┐     │
│  │                          分发调度层（V1.0简化版）                           │     │
│  │                                                                         │     │
│  │    ┌────────────────────────────────────────────────────────────────┐   │     │
│  │    │                     同步调用引擎 ⭐                               │   │     │
│  │    │   变更检测 → Service层直接调用 → 目标模块通知                     │   │     │
│  │    └────────────────────────────────────────────────────────────────┘   │     │
│  │                                                                         │     │
│  │    ┌────────────────────────────────────────────────────────────────┐   │     │
│  │    │                     定时同步引擎                                 │   │     │
│  │    │   定时触发 → 数据抽取 → 差异比对 → 同步执行                      │   │     │
│  │    └────────────────────────────────────────────────────────────────┘   │     │
│  │                                                                         │     │
│  └─────────────────────────────────────────────────────────────────────────┘     │
│                                       │                                              │
│                                       ▼                                              │
│  ┌─────────────────────────────────────────────────────────────────────────┐     │
│  │                     【V2.0升级方案】消息传输层                             │     │
│  │    ┌─────────────┐  ┌─────────────┐  ┌─────────────┐                  │     │
│  │    │  消息队列   │  │   可靠传输   │  │   消费确认   │                  │     │
│  │    │  (Kafka)    │  │             │  │             │                  │     │
│  │    └─────────────┘  └─────────────┘  └─────────────┘                  │     │
│  └─────────────────────────────────────────────────────────────────────────┘     │
│                                       │                                              │
│                                       ▼                                              │
│  ┌─────────────────────────────────────────────────────────────────────────┐     │
│  │                          业务消费层                                      │     │
│  │                                                                         │     │
│  │    ┌───────────┐  ┌───────────┐  ┌───────────┐  ┌───────────┐       │     │
│  │    │  生产管理  │  │  交易撮合  │  │ 供应链金融 │  │  政府监管  │       │     │
│  │    └───────────┘  └───────────┘  └───────────┘  └───────────┘       │     │
│  │                                                                         │     │
│  └─────────────────────────────────────────────────────────────────────────┘     │
│                                                                                 │
│  说明：V1.0采用单体Spring Boot架构，主数据变更通过Service层直接调用通知各模块，         │
│        预留事件驱动接口。V2.0拆分为微服务时，可无缝切换到Kafka消息队列。               │
│                                                                                 │
└─────────────────────────────────────────────────────────────────────────────────┘
```

### 6.2 分发模式说明

#### 6.2.1 同步调用模式（V1.0 MVP）

> **说明**：MVP阶段采用单体Spring Boot架构，主数据变更通过Service层直接调用通知各业务模块，无需引入消息队列。

| 特性 | 说明 |
|:-----|:-----|
| 触发条件 | 主数据变更时直接调用 |
| 时效性 | 同步执行（毫秒级） |
| 适用场景 | MVP阶段所有场景 |
| 技术实现 | Spring Event事件发布+监听器同步处理 |
| 扩展性 | 预留接口，V2.0可切换为Kafka |

**Spring Event实现示例**：

```java
// 1. 定义主数据变更事件
public class MasterDataChangeEvent extends ApplicationEvent {
    private String entityType;
    private String entityId;
    private String changeType; // CREATE/UPDATE/DELETE
    private Object changeData;
}

// 2. 主数据服务发布事件
@Service
public class UserMasterService {
    @Autowired
    private ApplicationEventPublisher publisher;
    
    public void createUser(User user) {
        // 保存数据
        userMapper.insert(user);
        // 发布变更事件
        publisher.publishEvent(new MasterDataChangeEvent(this, "user", user.getUserId(), "CREATE", user));
    }
}

// 3. 业务模块监听并处理
@Component
public class ProductionModuleListener {
    @EventListener
    public void handleUserChange(MasterDataChangeEvent event) {
        // 同步用户数据到生产模块
        productionService.syncUserData(event.getEntityId());
    }
}
```

#### 6.2.2 定时同步模式

| 特性 | 说明 |
|:-----|:-----|
| 触发条件 | 定时任务调度 |
| 时效性 | 分钟级/小时级 |
| 适用场景 | 批量数据同步、低实时性要求的场景 |
| 技术实现 | 定时任务+增量/全量数据抽取 |

#### 6.2.3 事件驱动模式（V2.0升级方案）

> **V2.0升级方案**：微服务架构下，通过Kafka消息队列实现事件驱动架构。

| 特性 | 说明 |
|:-----|:-----|
| 触发条件 | 主数据变更时自动触发 |
| 时效性 | 准实时（秒级延迟） |
| 适用场景 | V2.0微服务架构 |
| 技术实现 | Kafka消息队列+消费者组 |
| MVP兼容性 | V1.0预留接口，V2.0无缝切换 |

### 6.3 分发规则配置

**主数据分发规则表**

| 字段名 | 字段类型 | 说明 |
|:-------|:---------|:-----|
| rule_id | VARCHAR(32) | 分发规则ID |
| entity_type | VARCHAR(50) | 实体类型（用户/组织/商品等） |
| event_type | VARCHAR(50) | 事件类型（CREATE/UPDATE/DELETE） |
| target_modules | JSON | 目标模块列表 |
| sync_mode | ENUM | 同步模式：realtime/batch |
| sync_frequency | INT | 同步频率（分钟），仅batch模式 |
| filter_conditions | JSON | 分发条件过滤 |
| priority | INT | 优先级 |
| status | ENUM | 状态 |

### 6.4 分发监控页面

**页面路径**：`/master-data/distribution/monitor`

**页面布局**：
- 顶部：统计卡片（总分发量、成功数、失败数、延迟数）
- 左侧：分发任务列表
- 右侧：分发详情（状态、日志、进度）

**字段定义**：

| 字段名 | 类型 | 说明 |
|:-------|:-----|:-----|
| 分发任务ID | VARCHAR | 分发任务唯一标识 |
| 实体类型 | ENUM | 用户/组织/商品等 |
| 变更类型 | ENUM | 新增/修改/删除 |
| 目标模块 | JSON | 目标模块列表 |
| 分发状态 | ENUM | 等待中/进行中/成功/失败 |
| 分发时间 | TIMESTAMP | 分发触发时间 |
| 完成时间 | TIMESTAMP | 分发完成时间 |
| 延迟时间 | INT | 分发延迟（秒） |
| 错误信息 | VARCHAR | 失败原因 |

**操作按钮**：
- 重新分发
- 查看详情
- 导出日志

---

## 各主数据域详细设计

## 4.1 统一用户主数据

### 4.1.1 域定位与目标

用户主数据是平台所有用户实体的统一档案，涵盖平台各类用户的完整信息。用户主数据采用"一用户、多身份、统一管理"的设计理念，支持用户在不同业务场景下的身份切换和信息复用。

**核心目标**：
- 建立统一的用户身份标识，跨模块唯一
- 支持用户多身份配置，满足复杂业务场景
- 建立完善的用户画像体系，支撑精准服务
- 实现用户认证全流程管理，确保身份真实

### 4.1.2 页面清单

| 页面名称 | 路径 | 主要功能 | 用户角色 |
|:---------|:-----|:---------|:---------|
| 用户列表 | /master-data/user/list | 用户查询、筛选、批量操作 | 数据管理员 |
| 用户详情 | /master-data/user/detail/{id} | 用户信息查看、编辑 | 数据管理员 |
| 用户创建/编辑 | /master-data/user/form | 用户信息录入、修改 | 数据管理员 |
| 身份管理 | /master-data/user/{id}/identity | 身份配置、切换、认证 | 数据管理员、用户 |
| 用户画像 | /master-data/user/{id}/profile | 画像标签、数据展示 | 数据管理员 |
| 实名认证 | /master-data/user/authentication | 认证审核、证照管理 | 数据管理员 |
| 用户合并/拆分 | /master-data/user/merge | 重复用户合并、主从拆分 | 数据管理员 |

### 4.1.3 用户主数据管理页面

#### 4.1.3.1 用户列表页面

**页面路径**：`/master-data/user/list`

**页面布局**：
- 顶部：搜索栏（手机号、姓名、用户类型、状态）
- 工具栏：新建用户、批量导入、批量导出、批量冻结
- 中部：用户列表表格
- 底部：分页组件

**字段定义表**：

| 字段名 | 类型 | 必填 | 说明 |
|:-------|:-----|:-----|:-----|
| 用户ID | VARCHAR | 是 | 系统生成，唯一标识 |
| 用户编码 | VARCHAR | 是 | 手机号或系统编码 |
| 用户姓名 | VARCHAR | 是 | 真实姓名 |
| 用户类型 | ENUM | 是 | 个人/企业 |
| 证件类型 | ENUM | 否 | 身份证/营业执照 |
| 证件号码 | VARCHAR | 否 | 加密显示 |
| 手机号 | VARCHAR | 是 | 登录账号 |
| 所属组织 | VARCHAR | 否 | 关联组织名称 |
| 实名状态 | ENUM | 是 | 未认证/认证中/已认证/认证失败 |
| 用户状态 | ENUM | 是 | 正常/冻结/注销 |
| 注册时间 | TIMESTAMP | 是 | 创建时间 |
| 最后登录 | TIMESTAMP | 否 | 最近登录时间 |

**操作按钮**：
- 查看详情
- 编辑
- 实名认证
- 冻结/解冻
- 更多（删除、合并、导出）

**交互流程**：
1. 用户进入列表页，默认显示所有用户
2. 支持按手机号、姓名、用户类型、状态等条件筛选
3. 点击用户行可进入用户详情页
4. 批量操作需勾选后选择操作类型

**异常处理**：
- 网络异常：提示"网络连接失败，请重试"，保留用户输入
- 权限不足：提示"您没有该操作权限"，跳转登录页
- 数据加载失败：提示"加载失败"，显示重试按钮

#### 4.1.3.2 用户详情页面

**页面路径**：`/master-data/user/detail/{user_id}`

**页面布局**：
- 左侧：用户基本信息卡片
- 右侧Tab页：身份信息、认证记录、画像信息、操作日志

**基本信息字段定义表**：

| 字段名 | 类型 | 必填 | 说明 |
|:-------|:-----|:-----|:-----|
| 用户ID | VARCHAR | 是 | 系统生成 |
| 用户姓名 | VARCHAR | 是 | 真实姓名 |
| 用户类型 | ENUM | 是 | 个人/企业 |
| 性别 | ENUM | 否 | 男/女/未知 |
| 出生日期 | DATE | 否 | - |
| 手机号码 | VARCHAR | 是 | 登录账号 |
| 电子邮箱 | VARCHAR | 否 | 联系方式 |
| 省份/城市/区县 | VARCHAR | 否 | 地理位置 |
| 详细地址 | VARCHAR | 否 | - |
| 所属组织 | VARCHAR | 否 | 组织名称 |
| 实名认证状态 | ENUM | 是 | - |
| 风险等级 | ENUM | 否 | 低/中/高 |
| 信用评分 | INT | 否 | 0-1000 |
| 注册时间 | TIMESTAMP | 是 | - |
| 最后登录时间 | TIMESTAMP | 否 | - |

**操作按钮**：
- 编辑基本信息
- 冻结用户
- 重置密码
- 导出用户信息

#### 4.1.3.3 用户创建/编辑页面

**页面路径**：`/masterdata/user/form`

**页面布局**：
- 表单区域：基本信息、联系信息、认证信息
- 底部：保存、取消按钮

**字段定义表**：

| 字段名 | 类型 | 必填 | 说明 |
|:-------|:-----|:-----|:-----|
| 用户姓名 | VARCHAR(100) | 是 | 真实姓名 |
| 用户类型 | ENUM | 是 | 个人/企业 |
| 性别 | ENUM | 否 | 男/女/未知 |
| 出生日期 | DATE | 否 | - |
| 手机号码 | VARCHAR(20) | 是 | 唯一，不可重复 |
| 电子邮箱 | VARCHAR(100) | 否 | 邮箱格式校验 |
| 证件类型 | ENUM | 是 | 身份证/营业执照 |
| 证件号码 | VARCHAR(50) | 是 | 加密存储 |
| 省份 | VARCHAR(50) | 否 | 下拉选择 |
| 城市 | VARCHAR(50) | 否 | 下拉选择 |
| 区县 | VARCHAR(50) | 否 | 下拉选择 |
| 详细地址 | VARCHAR(200) | 否 | - |
| 所属组织 | SELECT | 否 | 组织树选择 |

**业务规则**：
- 手机号格式：11位数字，以1开头
- 证件号码：个人身份证18位，企业营业执照18位统一社会信用代码
- 同一手机号不可重复注册
- 敏感字段（证件号码）加密存储

### 4.1.4 用户身份管理页面

**页面路径**：`/master-data/user/{user_id}/identity`

**页面布局**：
- 当前身份卡片（主身份展示）
- 身份列表（支持的身份类型）
- 添加身份按钮

**身份类型枚举**：

| 身份代码 | 身份名称 | 适用场景 |
|:---------|:---------|:---------|
| farmer | 农户 | 种植户、养殖户 |
| collector | 收购商 | 农产品经纪人、代办 |
| wholesaler | 批发商 | 一级批发、二级批发 |
| retailer | 零售商 | 商超、门店 |
| service_provider | 服务商 | 农机、农技、保险 |
| government | 政府用户 | 农业农村局、市场监管局等 |
| enterprise | 企业用户 | 加工企业、贸易公司 |
| admin | 平台管理员 | 平台运营人员 |

**身份信息字段定义表**：

| 字段名 | 类型 | 必填 | 说明 |
|:-------|:-----|:-----|:-----|
| 身份ID | VARCHAR(32) | 是 | 系统生成 |
| 用户ID | VARCHAR(32) | 是 | 外键关联 |
| 身份类型 | ENUM | 是 | 见身份类型枚举 |
| 身份名称 | VARCHAR | 是 | 显示名称 |
| 身份状态 | ENUM | 是 | 有效/无效 |
| 认证时间 | TIMESTAMP | 否 | 最后认证时间 |
| 认证有效期 | TIMESTAMP | 否 | 过期时间 |
| 身份扩展数据 | JSON | 否 | 身份相关扩展信息 |

**交互流程**：
1. 用户可选择一个主身份
2. 支持添加多个副身份
3. 切换身份时需二次确认
4. 身份认证状态影响身份有效性

### 4.1.5 用户画像管理页面

**页面路径**：`/master-data/user/{user_id}/profile`

**页面布局**：
- 左侧：画像标签树
- 右侧：标签详情、数据统计

**画像类型**：

| 画像类型 | 说明 |
|:---------|:-----|
| 基础画像 | 用户基本信息标签 |
| 经营画像 | 经营规模、经营品类等 |
| 行为画像 | 活跃度、偏好等 |

### 4.1.6 实名认证管理页面

**页面路径**：`/master-data/user/authentication`

**页面布局**：
- 待审核列表
- 认证详情（证照图片、基本信息）
- 审核操作（通过/拒绝）

**认证类型**：

| 认证类型 | 认证内容 | 认证方式 | 适用用户 |
|:---------|:---------|:---------|:---------|
| 个人实名认证 | 姓名+身份证+人脸识别 | 公安库比对+活体检测 | 所有个人用户 |
| 企业实名认证 | 企业名称+统一社会信用代码+法人信息 | 工商数据自动比对 | 企业用户 |

### 4.1.7 核心API接口清单

| 接口名称 | 请求方式 | 路径 | 主要参数 | 返回说明 |
|:---------|:---------|:-----|:---------|:---------|
| 用户列表查询 | GET | /api/master-data/users | page, size, params | 分页用户列表 |
| 用户详情查询 | GET | /api/master-data/users/{id} | userId | 用户详细信息 |
| 用户创建 | POST | /api/master-data/users | userInfo | 创建结果 |
| 用户更新 | PUT | /api/master-data/users/{id} | userId, userInfo | 更新结果 |
| 用户删除 | DELETE | /api/master-data/users/{id} | userId | 删除结果 |
| 身份列表查询 | GET | /api/master-data/users/{id}/identities | userId | 身份列表 |
| 身份创建 | POST | /api/master-data/users/{id}/identities | identityInfo | 创建结果 |
| 身份切换 | PUT | /api/master-data/users/{id}/identities/switch | identityId | 切换结果 |
| 画像查询 | GET | /api/master-data/users/{id}/profile | userId | 画像信息 |
| 认证申请 | POST | /api/master-data/authentications | authInfo | 申请结果 |
| 认证审核 | PUT | /api/master-data/authentications/{id}/review | authId, result | 审核结果 |
| 用户合并 | POST | /api/master-data/users/merge | sourceIds, targetId | 合并结果 |

### 4.1.8 数据模型设计

#### user_master（用户主数据表）

| 字段名 | 字段类型 | 长度 | 说明 | 示例 |
|:-------|:---------|:-----|:-----|:-----|
| user_id | VARCHAR | 32 | 用户唯一标识，主键 | USR20250100001 |
| user_code | VARCHAR | 50 | 用户编码，唯一索引 | 13800138000 |
| user_name | VARCHAR | 100 | 用户姓名/名称 | 张三 |
| user_type | ENUM | - | 用户大类：personal/enterprise | personal |
| id_card_type | ENUM | - | 证件类型：identity_card/business_license | identity_card |
| id_card_no | VARCHAR | 50 | 证件号码，加密存储 | 110101199001011234 |
| phone | VARCHAR | 20 | 手机号码 | 13800138000 |
| email | VARCHAR | 100 | 电子邮箱 | zhangsan@example.com |
| avatar_url | VARCHAR | 500 | 头像URL | https://... |
| gender | ENUM | - | 性别：male/female/unknown | male |
| birthday | DATE | - | 出生日期 | 1990-01-01 |
| province | VARCHAR | 50 | 省份 | 北京市 |
| city | VARCHAR | 50 | 城市 | 北京市 |
| district | VARCHAR | 50 | 区县 | 海淀区 |
| address | VARCHAR | 200 | 详细地址 | xx街道xx号 |
| org_id | VARCHAR | 32 | 所属组织ID，外键 | ORG20250100001 |
| user_status | ENUM | - | 用户状态：active/frozen/cancelled | active |
| real_name_status | ENUM | - | 实名认证状态 | verified |
| risk_level | ENUM | - | 风险等级：low/medium/high | low |
| credit_score | INT | - | 信用评分，0-1000 | 750 |
| last_login_time | TIMESTAMP | - | 最后登录时间 | 2025-01-15 10:00:00 |
| last_login_ip | VARCHAR | 50 | 最后登录IP | 192.168.1.1 |
| create_time | TIMESTAMP | - | 创建时间 | 2025-01-01 00:00:00 |
| update_time | TIMESTAMP | - | 更新时间 | 2025-01-15 10:00:00 |
| create_by | VARCHAR | 32 | 创建人 | system |
| update_by | VARCHAR | 32 | 更新人 | admin |

#### user_identity（用户身份表）

| 字段名 | 字段类型 | 长度 | 说明 | 示例 |
|:-------|:---------|:-----|:-----|:-----|
| identity_id | VARCHAR | 32 | 身份记录ID，主键 | IDT20250100001 |
| user_id | VARCHAR | 32 | 用户ID，外键 | USR20250100001 |
| identity_type | ENUM | - | 身份类型 | farmer |
| identity_name | VARCHAR | 100 | 身份名称 | 种植户 |
| identity_status | ENUM | - | 身份状态：active/inactive | active |
| identity_data | JSON | - | 身份扩展数据 | {...} |
| verified_time | TIMESTAMP | - | 认证时间 | 2025-01-01 |
| expire_time | TIMESTAMP | - | 认证有效期 | 2026-01-01 |
| create_time | TIMESTAMP | - | 创建时间 | 2025-01-01 |

#### user_profile（用户画像表）

| 字段名 | 字段类型 | 长度 | 说明 | 示例 |
|:-------|:---------|:-----|:-----|:-----|
| profile_id | VARCHAR | 32 | 画像ID，主键 | PFL20250100001 |
| user_id | VARCHAR | 32 | 用户ID，外键 | USR20250100001 |
| profile_type | ENUM | - | 画像类型：basic/business/behavior | basic |
| profile_data | JSON | - | 画像数据 | {...} |
| tags | JSON | - | 用户标签 | ["种植大户","优质供应商"] |
| score_data | JSON | - | 评分数据 | {"credit": 850} |
| update_time | TIMESTAMP | - | 更新时间 | 2025-01-15 |

#### user_authentication（用户认证表）

| 字段名 | 字段类型 | 长度 | 说明 | 示例 |
|:-------|:---------|:-----|:-----|:-----|
| auth_id | VARCHAR | 32 | 认证记录ID，主键 | AUTH20250100001 |
| user_id | VARCHAR | 32 | 用户ID | USR20250100001 |
| auth_type | ENUM | - | 认证类型：real_name/enterprise/license | real_name |
| auth_status | ENUM | - | 认证状态 | pending |
| auth_level | INT | - | 认证级别 | 2 |
| auth_data | JSON | - | 认证资料 | {...} |
| auth_result | JSON | - | 认证结果 | {...} |
| verified_by | VARCHAR | 32 | 审核人 | admin |
| verified_time | TIMESTAMP | - | 审核时间 | 2025-01-15 |
| create_time | TIMESTAMP | - | 创建时间 | 2025-01-01 |

### 4.1.9 业务规则与约束

1. **唯一性约束**：同一手机号不可重复注册
2. **密码安全**：密码至少8位，包含数字和字母
3. **实名认证**：涉及交易功能必须完成实名认证
4. **敏感数据**：证件号码、银行卡号等敏感字段加密存储
5. **数据同步**：用户状态变更需同步至各业务模块
6. **合并限制**：已被业务引用的用户不可合并

---

## 4.2 组织主数据

### 4.2.1 域定位与目标

组织主数据管理平台各类组织机构的信息，包括企业、合作社、家庭农场、政府机构等类型，为业务协同和权限管理提供组织基础。

**核心目标**：
- 建立统一的组织架构体系
- 支持多层级组织管理
- 实现组织认证与资质管理
- 支撑组织内人员权限管理

### 4.2.2 页面清单

| 页面名称 | 路径 | 主要功能 | 用户角色 |
|:---------|:-----|:---------|:---------|
| 组织列表 | /master-data/org/list | 组织查询、树形展示 | 数据管理员 |
| 组织详情 | /master-data/org/detail/{id} | 组织信息查看、编辑 | 数据管理员 |
| 组织创建/编辑 | /master-data/org/form | 组织信息录入、修改 | 数据管理员 |
| 部门管理 | /master-data/org/{id}/dept | 部门增删改查 | 数据管理员 |
| 岗位管理 | /master-data/org/{id}/position | 岗位配置 | 数据管理员 |
| 组织变更 | /master-data/org/change | 合并、拆分、调整 | 数据管理员 |
| 组织认证 | /master-data/org/{id}/certification | 资质认证管理 | 数据管理员 |

### 4.2.3 组织架构管理页面

#### 4.2.3.1 组织树页面

**页面路径**：`/master-data/org/list`

**页面布局**：
- 左侧：组织树形结构
- 右侧：选中组织详情

**组织类型枚举**：

| 组织类型代码 | 组织类型名称 | 适用场景 |
|:-------------|:-------------|:---------|
| enterprise | 企业 | 农业企业、贸易公司 |
| cooperative | 合作社 | 农民专业合作社 |
| family_farm | 家庭农场 | 规模农户 |
| government | 政府机构 | 农业农村局等 |
| institution | 事业单位 | 农科院所 |
| association | 社会组织 | 行业协会 |

**字段定义表**：

| 字段名 | 类型 | 必填 | 说明 |
|:-------|:-----|:-----|:-----|
| 组织ID | VARCHAR | 是 | 系统生成 |
| 组织编码 | VARCHAR | 是 | 营业执照号/统一社会信用代码 |
| 组织名称 | VARCHAR | 是 | - |
| 组织类型 | ENUM | 是 | - |
| 上级组织 | VARCHAR | 否 | 父组织ID |
| 法人代表 | VARCHAR | 否 | 企业/合作社必填 |
| 联系电话 | VARCHAR | 否 | - |
| 所在地区 | VARCHAR | 否 | 省市区 |
| 组织状态 | ENUM | 是 | 正常/停业/注销 |

**操作按钮**：
- 新增下级组织
- 编辑组织
- 查看详情
- 认证管理
- 停用/启用

#### 4.2.3.2 组织详情页面

**页面路径**：`/master-data/org/detail/{org_id}`

**页面布局**：
- 基本信息Tab
- 成员列表Tab
- 关联业务Tab
- 操作日志Tab

**基本信息字段定义表**：

| 字段名 | 类型 | 必填 | 说明 |
|:-------|:-----|:-----|:-----|
| 组织ID | VARCHAR | 是 | 系统生成 |
| 组织编码 | VARCHAR | 是 | 唯一标识 |
| 组织名称 | VARCHAR | 是 | - |
| 组织类型 | ENUM | 是 | - |
| 法人代表 | VARCHAR | 否 | - |
| 联系人 | VARCHAR | 否 | - |
| 联系电话 | VARCHAR | 否 | - |
| 联系手机 | VARCHAR | 否 | - |
| 省份/城市/区县 | VARCHAR | 否 | - |
| 详细地址 | VARCHAR | 否 | - |
| 经纬度 | DECIMAL | 否 | 位置信息 |
| 注册资本 | DECIMAL | 否 | 万元 |
| 经营范围 | TEXT | 否 | - |
| 营业执照号 | VARCHAR | 否 | - |
| 税务登记号 | VARCHAR | 否 | - |
| 风险等级 | ENUM | 否 | - |
| 信用等级 | ENUM | 否 | AAA/AA/A/B/C |
| 成员数量 | INT | 否 | - |

#### 4.2.3.3 部门管理页面

**页面路径**：`/master-data/org/{org_id}/dept`

**页面布局**：
- 部门树形结构
- 部门详情面板

**字段定义表**：

| 字段名 | 类型 | 必填 | 说明 |
|:-------|:-----|:-----|:-----|
| 部门ID | VARCHAR | 是 | - |
| 部门编码 | VARCHAR | 是 | - |
| 部门名称 | VARCHAR | 是 | - |
| 上级部门 | VARCHAR | 否 | - |
| 部门负责人 | VARCHAR | 否 | - |
| 联系电话 | VARCHAR | 否 | - |
| 部门人数 | INT | 否 | - |
| 状态 | ENUM | 是 | - |

#### 4.2.3.4 岗位管理页面

**页面路径**：`/masterdata/org/{org_id}/position`

**页面布局**：
- 岗位列表
- 新增/编辑表单

**字段定义表**：

| 字段名 | 类型 | 必填 | 说明 |
|:-------|:-----|:-----|:-----|
| 岗位ID | VARCHAR | 是 | - |
| 岗位编码 | VARCHAR | 是 | - |
| 岗位名称 | VARCHAR | 是 | - |
| 所属部门 | VARCHAR | 是 | - |
| 岗位级别 | INT | 否 | - |
| 岗位职责 | TEXT | 否 | - |
| 任职人数 | INT | 否 | - |
| 状态 | ENUM | 是 | - |

### 4.2.4 核心API接口清单

| 接口名称 | 请求方式 | 路径 | 主要参数 | 返回说明 |
|:---------|:---------|:-----|:---------|:---------|
| 组织树查询 | GET | /api/master-data/orgs/tree | - | 组织树结构 |
| 组织列表查询 | GET | /api/master-data/orgs | page, size, params | 分页组织列表 |
| 组织详情查询 | GET | /api/master-data/orgs/{id} | orgId | 组织详细信息 |
| 组织创建 | POST | /api/master-data/orgs | orgInfo | 创建结果 |
| 组织更新 | PUT | /api/master-data/orgs/{id} | orgId, orgInfo | 更新结果 |
| 组织删除 | DELETE | /api/master-data/orgs/{id} | orgId | 删除结果 |
| 部门列表 | GET | /api/master-data/orgs/{id}/depts | orgId | 部门列表 |
| 部门创建 | POST | /api/master-data/orgs/{id}/depts | deptInfo | 创建结果 |
| 岗位列表 | GET | /api/master-data/orgs/{id}/positions | orgId | 岗位列表 |
| 岗位创建 | POST | /api/master-data/orgs/{id}/positions | positionInfo | 创建结果 |
| 组织认证 | POST | /api/master-data/orgs/{id}/certifications | certInfo | 认证结果 |

### 4.2.5 数据模型设计

#### organization（组织主数据表）

| 字段名 | 字段类型 | 长度 | 说明 | 示例 |
|:-------|:---------|:-----|:-----|:-----|
| org_id | VARCHAR | 32 | 组织唯一标识，主键 | ORG20250100001 |
| org_code | VARCHAR | 50 | 组织编码 | 91110108MA01XXXX |
| org_name | VARCHAR | 200 | 组织名称 | 北京xx农业科技有限公司 |
| org_type | ENUM | - | 组织类型 | enterprise |
| parent_org_id | VARCHAR | 32 | 上级组织ID | ORG20250100000 |
| org_level | INT | - | 组织层级深度 | 1 |
| org_path | VARCHAR | 500 | 组织路径，层级结构 | /root/holding/company |
| legal_person | VARCHAR | 100 | 法人代表 | 李四 |
| contact_person | VARCHAR | 100 | 联系人 | 王五 |
| contact_phone | VARCHAR | 20 | 联系电话 | 010-12345678 |
| contact_mobile | VARCHAR | 20 | 联系手机 | 13800138000 |
| province | VARCHAR | 50 | 省份 | 北京市 |
| city | VARCHAR | 50 | 城市 | 北京市 |
| district | VARCHAR | 50 | 区县 | 海淀区 |
| address | VARCHAR | 200 | 详细地址 | xx路xx号 |
| longitude | DECIMAL | 10,7 | 经度 | 116.407397 |
| latitude | DECIMAL | 10,7 | 纬度 | 39.904211 |
| registered_capital | DECIMAL | 15,2 | 注册资本（万元） | 1000.00 |
| business_scope | TEXT | - | 经营范围 | 农产品种植、销售... |
| business_license | VARCHAR | 50 | 营业执照号 | 91110108MA01XXXX |
| tax_no | VARCHAR | 50 | 税务登记号 | 110105xxxxxxxxxx |
| org_status | ENUM | - | 组织状态：active/suspended/cancelled | active |
| risk_level | ENUM | - | 风险等级 | low |
| credit_level | ENUM | - | 信用等级 | AAA |
| member_count | INT | - | 成员数量 | 50 |
| create_time | TIMESTAMP | - | 创建时间 | 2025-01-01 |
| update_time | TIMESTAMP | - | 更新时间 | 2025-01-15 |

#### org_hierarchy（组织层级关系表）

| 字段名 | 字段类型 | 长度 | 说明 | 示例 |
|:-------|:---------|:-----|:-----|:-----|
| hierarchy_id | VARCHAR | 32 | 层级关系ID，主键 | HIER20250100001 |
| org_id | VARCHAR | 32 | 当前组织 | ORG20250100001 |
| parent_org_id | VARCHAR | 32 | 上级组织 | ORG20250100000 |
| ancestor_org_id | VARCHAR | 32 | 祖先组织 | ORG20250100000 |
| depth | INT | - | 层级深度 | 2 |
| path | VARCHAR | 500 | 路径编码 | /ORG001/ORG002 |

#### org_certification（组织认证表）

| 字段名 | 字段类型 | 长度 | 说明 | 示例 |
|:-------|:---------|:-----|:-----|:-----|
| cert_id | VARCHAR | 32 | 认证ID，主键 | CERT20250100001 |
| org_id | VARCHAR | 32 | 组织ID | ORG20250100001 |
| cert_type | ENUM | - | 认证类型 | business_license |
| cert_name | VARCHAR | 100 | 认证名称 | 营业执照 |
| cert_no | VARCHAR | 50 | 证书编号 | 91110108MA01XXXX |
| issue_org | VARCHAR | 100 | 发证机构 | 北京市市场监管局 |
| issue_date | DATE | - | 发证日期 | 2020-01-01 |
| expire_date | DATE | - | 过期日期 | 2030-01-01 |
| cert_file | VARCHAR | 500 | 证书文件URL | https://... |
| cert_status | ENUM | - | 认证状态 | valid |

### 4.2.6 业务规则与约束

1. **组织编码唯一性**：同一组织编码不可重复
2. **层级限制**：组织层级最多支持10级
3. **叶子节点限制**：有下级组织的组织不可直接删除
4. **认证有效期**：认证过期前30天提醒
5. **数据同步**：组织变更需同步至权限系统

---

## 4.3 权限主数据

### 4.3.1 域定位与目标

本平台采用基于角色的访问控制（RBAC）模型，实现用户权限的精细化管理。RBAC模型包含用户（User）、角色（Role）、权限（Permission）三个核心要素，通过角色-权限关联和用户-角色关联实现灵活的权限配置。

**核心目标**：
- 建立完整的RBAC权限体系
- 支持菜单、功能、数据三个维度的权限控制
- 实现权限配置的可视化管理
- 提供权限变更的审计追溯

### 4.3.2 页面清单

| 页面名称 | 路径 | 主要功能 | 用户角色 |
|:---------|:-----|:---------|:---------|
| 角色管理 | /master-data/permission/role | 角色定义、权限配置、成员管理 | 系统管理员 |
| 权限资源 | /master-data/permission/resource | 菜单、功能、API权限配置 | 系统管理员 |
| 数据权限 | /master-data/permission/data | 数据隔离规则配置 | 系统管理员 |
| 权限审计 | /master-data/permission/audit | 权限变更日志 | 审计员 |

### 4.3.3 角色管理页面

#### 4.3.3.1 角色列表页面

**页面路径**：`/master-data/permission/role`

**页面布局**：
- 角色列表（卡片/表格）
- 新增角色按钮

**预置角色清单**：

| 角色编码 | 角色名称 | 角色类型 | 权限范围 |
|:---------|:---------|:---------|:---------|
| SUPER_ADMIN | 超级管理员 | system | 全部权限 |
| PLATFORM_ADMIN | 平台管理员 | system | 平台运营管理 |
| TENANT_ADMIN | 租户管理员 | system | 本租户全部数据管理 |
| TENANT_USER | 租户用户 | system | 本租户数据操作 |
| GOVERNMENT_ADMIN | 政府管理员 | system | 政府监管功能 |
| PRODUCTION_ADMIN | 生产管理员 | business | 生产管理模块全部 |
| PRODUCTION_USER | 生产用户 | business | 生产管理模块-操作 |
| TRADE_ADMIN | 交易管理员 | business | 交易撮合模块全部 |
| TRADE_USER | 交易用户 | business | 交易撮合模块-操作 |
| FINANCE_ADMIN | 金融管理员 | business | 供应链金融全部 |
| FINANCE_AUDITOR | 金融审核员 | business | 供应链金融-审核 |
| SERVICE_ADMIN | 服务管理员 | business | 综合服务模块全部 |
| S2B2C_ADMIN | S2B2C管理员 | business | S2B2C模块全部 |
| GOV_MONITOR | 监管用户 | business | 政府监管-查看 |
| GOV_ANALYST | 数据分析员 | business | 政府监管-分析 |

**角色类型说明**：

| 角色类型 | 说明 | 示例 |
|:---------|:-----|:-----|
| system | 系统预置角色，不可修改删除 | 超级管理员、运营管理员 |
| business | 业务角色，绑定业务模块 | 生产管理员、交易专员、金融审核 |
| custom | 自定义角色，管理员可创建 | 合作社社长、批发商业务员 |

**字段定义表**：

| 字段名 | 类型 | 必填 | 说明 |
|:-------|:-----|:-----|:-----|
| 角色ID | VARCHAR | 是 | 系统生成 |
| 角色编码 | VARCHAR | 是 | 唯一标识 |
| 角色名称 | VARCHAR | 是 | - |
| 角色类型 | ENUM | 是 | system/business/custom |
| 父角色ID | VARCHAR | 否 | 角色层级 |
| 角色描述 | VARCHAR | 否 | - |
| 角色级别 | INT | 否 | - |
| 状态 | ENUM | 是 | 启用/停用 |
| 是否系统内置 | BOOLEAN | 是 | - |
| 成员数量 | INT | 否 | - |

**操作按钮**：
- 查看
- 编辑（仅custom类型）
- 权限配置
- 成员管理
- 停用/启用
- 删除（仅custom类型）

#### 4.3.3.2 权限配置页面

**页面路径**：`/master-data/permission/role/{id}/config`

**页面布局**：
- 左侧：权限树
- 右侧：选中权限详情

**权限树结构示例**：

```
系统管理 (system)
├── 用户管理 (system:user)
│   ├── 用户查询 (system:user:list)
│   ├── 用户新增 (system:user:add)
│   ├── 用户编辑 (system:user:edit)
│   ├── 用户删除 (system:user:delete)
│   └── 重置密码 (system:user:resetPwd)
├── 角色管理 (system:role)
│   ├── 角色查询 (system:role:list)
│   ├── 角色新增 (system:role:add)
│   ├── 角色编辑 (system:role:edit)
│   └── 角色删除 (system:role:delete)
└── 权限管理 (system:permission)
    ├── 权限查询 (system:permission:list)
    └── 权限配置 (system:permission:config)

生产管理 (production)
├── 农场管理 (production:farm)
│   ├── 农场查询 (production:farm:list)
│   ├── 农场新增 (production:farm:add)
│   ├── 农场编辑 (production:farm:edit)
│   └── 农场删除 (production:farm:delete)
├── 地块管理 (production:plot)
│   ├── 地块查询 (production:plot:list)
│   └── 地块操作 (production:plot:operate)
└── ...

交易撮合 (trade)
├── 商品管理 (trade:product)
│   ├── 商品查询 (trade:product:list)
│   ├── 商品发布 (trade:product:add)
│   └── 商品上下架 (trade:product:status)
├── 订单管理 (trade:order)
│   ├── 订单查询 (trade:order:list)
│   └── 订单处理 (trade:order:process)
└── ...
```

**权限类型说明**：

| 权限类型 | 说明 | 示例 |
|:---------|:-----|:-----|
| menu | 菜单权限 | 页面访问控制 |
| action | 功能权限 | 增删改查操作 |
| data | 数据权限 | 数据范围控制 |

### 4.3.4 数据权限配置页面

**页面路径**：`/master-data/permission/data`

**页面布局**：
- 角色选择
- 数据权限配置面板

**数据权限范围说明**：

| 范围代码 | 范围名称 | 说明 |
|:---------|:---------|:-----|
| ALL | 全部数据 | 可查看所有数据（仅超级管理员） |
| TENANT | 本租户数据 | 仅可查看所属租户的全部数据（租户管理员级别） |
| ORG | 本组织数据 | 仅可查看所属组织及下级组织数据 |
| DEPT | 本部门数据 | 仅可查看所属部门数据 |
| OWNER | 本人数据 | 仅可查看本人创建的数据 |

### 4.3.5 核心API接口清单

| 接口名称 | 请求方式 | 路径 | 主要参数 | 返回说明 |
|:---------|:---------|:-----|:---------|:---------|
| 角色列表 | GET | /api/v1/masterdata/roles | page, size, type | 角色列表 |
| 角色详情 | GET | /api/v1/masterdata/roles/{id} | roleId | 角色信息 |
| 角色创建 | POST | /api/v1/masterdata/roles | roleInfo | 创建结果 |
| 角色更新 | PUT | /api/v1/masterdata/roles/{id} | roleId, roleInfo | 更新结果 |
| 角色删除 | DELETE | /api/v1/masterdata/roles/{id} | roleId | 删除结果 |
| 权限树查询 | GET | /api/v1/masterdata/permissions/tree | - | 权限树 |
| 角色权限查询 | GET | /api/v1/masterdata/roles/{id}/permissions | roleId | 权限列表 |
| 角色权限配置 | PUT | /api/v1/masterdata/roles/{id}/permissions | roleId, permIds | 配置结果 |
| 权限校验 | POST | /api/v1/masterdata/permissions/check | userId, permission | 校验结果 |
| 用户角色查询 | GET | /api/v1/masterdata/users/{id}/roles | userId | 角色列表 |
| 用户角色绑定 | POST | /api/v1/masterdata/users/{id}/roles | userId, roleIds | 绑定结果 |
| 审计日志查询 | GET | /api/v1/masterdata/permissions/audit | params | 日志列表 |

### 4.3.6 RBAC权限模型设计

```
┌─────────────────────────────────────────────────────────────────────────────┐
│                           RBAC权限模型架构                                    │
├─────────────────────────────────────────────────────────────────────────────┤
│                                                                             │
│                          ┌─────────────┐                                    │
│                          │   用户 User  │                                    │
│                          └──────┬──────┘                                    │
│                                 │                                            │
│                          ┌──────┴──────┐                                    │
│                          │ user_role   │  (多对多关联)                        │
│                          │ 用户-角色表  │                                    │
│                          └──────┬──────┘                                    │
│                                 │                                            │
│                          ┌──────┴──────┐                                    │
│                          │   角色 Role  │                                    │
│                          └──────┬──────┘                                    │
│                                 │                                            │
│                          ┌──────┴──────┐                                    │
│                          │role_permis  │  (多对多关联)                        │
│                          │角色-权限表  │                                    │
│                          └──────┬──────┘                                    │
│                                 │                                            │
│                          ┌──────┴──────┐                                    │
│                          │权限 Permission│                                   │
│                          └─────────────┘                                    │
│                                                                             │
│  ┌───────────────────────────────────────────────────────────────────────┐ │
│  │                           权限类型细分                                  │ │
│  │                                                                       │ │
│  │   ┌─────────────┐  ┌─────────────┐  ┌─────────────┐                   │ │
│  │   │ 菜单权限    │  │ 功能权限    │  │ 数据权限    │                   │ │
│  │   │ Menu        │  │ Action      │  │ Data        │                   │ │
│  │   │ 页面访问    │  │ 操作执行    │  │ 范围控制    │                   │ │
│  │   └─────────────┘  └─────────────┘  └─────────────┘                   │ │
│  │                                                                       │ │
│  └───────────────────────────────────────────────────────────────────────┘ │
│                                                                             │
│  ┌───────────────────────────────────────────────────────────────────────┐ │
│  │                           数据权限范围层级                              │ │
│  │                                                                       │ │
│  │   全部(ALL) ──► 本组织(ORG) ──► 本部门(DEPT) ──► 本人(OWNER)          │ │
│  │                                                                       │ │
│  └───────────────────────────────────────────────────────────────────────┘ │
│                                                                             │
└─────────────────────────────────────────────────────────────────────────────┘
```

#### role（角色表）

| 字段名 | 字段类型 | 长度 | 说明 | 示例 |
|:-------|:---------|:-----|:-----|:-----|
| role_id | VARCHAR | 32 | 角色ID，主键 | ROLE20250100001 |
| role_code | VARCHAR | 50 | 角色编码 | PLATFORM_ADMIN |
| role_name | VARCHAR | 100 | 角色名称 | 平台管理员 |
| role_type | ENUM | - | 角色类型：system/business/custom | system |
| role_level | INT | - | 角色级别 | 1 |
| parent_role_id | VARCHAR | 32 | 父角色ID | ROLE20250100000 |
| role_desc | VARCHAR | 500 | 角色描述 | - |
| role_status | ENUM | - | 状态：enabled/disabled | enabled |
| is_system | TINYINT | - | 是否系统内置：0/1 | 1 |
| sort_order | INT | - | 排序号 | 1 |
| create_time | TIMESTAMP | - | 创建时间 | 2025-01-01 |

#### permission（权限表）

| 字段名 | 字段类型 | 长度 | 说明 | 示例 |
|:-------|:---------|:-----|:-----|:-----|
| permission_id | VARCHAR | 32 | 权限ID，主键 | PERM20250100001 |
| permission_code | VARCHAR | 100 | 权限编码 | system:user:list |
| permission_name | VARCHAR | 100 | 权限名称 | 用户查询 |
| permission_type | ENUM | - | 权限类型：menu/action/data | action |
| parent_id | VARCHAR | 32 | 父权限ID | PERM20250100000 |
| permission_path | VARCHAR | 200 | 权限路径 | /system/user |
| permission_icon | VARCHAR | 50 | 图标 | user |
| permission_url | VARCHAR | 200 | URL地址 | /api/users |
| component | VARCHAR | 100 | 前端组件 | UserList |
| is_leaf | TINYINT | - | 是否叶子节点 | 1 |
| sort_order | INT | - | 排序号 | 1 |
| status | ENUM | - | 状态 | enabled |
| create_time | TIMESTAMP | - | 创建时间 | 2025-01-01 |

#### role_permission（角色权限关联表）

| 字段名 | 字段类型 | 长度 | 说明 | 示例 |
|:-------|:---------|:-----|:-----|:-----|
| id | BIGINT | - | 主键 | 1 |
| role_id | VARCHAR | 32 | 角色ID | ROLE20250100001 |
| permission_id | VARCHAR | 32 | 权限ID | PERM20250100001 |
| create_time | TIMESTAMP | - | 创建时间 | 2025-01-01 |

#### user_role（用户角色关联表）

| 字段名 | 字段类型 | 长度 | 说明 | 示例 |
|:-------|:---------|:-----|:-----|:-----|
| id | BIGINT | - | 主键 | 1 |
| user_id | VARCHAR | 32 | 用户ID | USR20250100001 |
| role_id | VARCHAR | 32 | 角色ID | ROLE20250100001 |
| org_id | VARCHAR | 32 | 所属组织（多租户支持） | ORG20250100001 |
| create_time | TIMESTAMP | - | 创建时间 | 2025-01-01 |

#### data_permission（数据权限表）

| 字段名 | 字段类型 | 长度 | 说明 | 示例 |
|:-------|:---------|:-----|:-----|:-----|
| dp_id | VARCHAR | 32 | 数据权限ID，主键 | DP20250100001 |
| role_id | VARCHAR | 32 | 角色ID | ROLE20250100001 |
| data_scope | ENUM | - | 数据范围：ALL/TENANT/ORG/DEPT/OWNER | TENANT |
| custom_depts | JSON | - | 自定义部门范围 | [dept1, dept2] |
| data_conditions | JSON | - | 自定义数据条件 | {...} |
| module_type | VARCHAR | 50 | 业务模块类型 | production |
| create_time | TIMESTAMP | - | 创建时间 | 2025-01-01 |

### 4.3.7 业务规则与约束

1. **系统角色不可修改**：预置system类型角色不可编辑/删除
2. **互斥角色检查**：用户不可同时拥有互斥角色
3. **权限继承**：子角色自动继承父角色权限
4. **数据权限限制**：数据权限不得超出角色级别允许范围
5. **审计留痕**：所有权限变更必须记录审计日志

---

## 4.4 商品主数据

### 4.4.1 域定位与目标

商品主数据管理农产品的标准化信息，包括分类体系、属性模板、质量标准、编码规则等，为交易撮合、供应链管理等业务提供标准化的商品数据基础。

**核心目标**：
- 建立农产品分类标准体系
- 实现商品属性的模板化管理
- 建立商品质量等级标准
- 统一商品编码规则

### 4.4.2 页面清单

| 页面名称 | 路径 | 主要功能 | 用户角色 |
|:---------|:-----|:---------|:---------|
| 商品分类 | /masterdata/product/category | 分类树、分类属性 | 数据管理员 |
| 标准库 | /masterdata/product/standard | SPU管理、规格定义、属性模板 | 数据管理员 |
| 商品审核 | /masterdata/product/audit | 商品审核流程 | 数据管理员 |
| 编码规则 | /masterdata/product/code-rule | SKU/SPU编码规则配置 | 系统管理员 |
| 质量标准 | /masterdata/product/quality | 质量等级标准管理 | 数据管理员 |

### 4.4.3 商品分类管理页面

**页面路径**：`/master-data/product/category`

**页面布局**：
- 左侧：分类树形结构
- 右侧：选中分类详情、属性配置

**商品分类示例**：

```
农产品
├── 粮食作物
│   ├── 稻谷
│   │   ├── 籼稻
│   │   │   ├── 早籼稻
│   │   │   └── 晚籼稻
│   │   └── 粳稻
│   │       ├── 早粳稻
│   │       └── 晚粳稻
│   ├── 小麦
│   │   ├── 硬粒小麦
│   │   └── 软粒小麦
│   └── 玉米
│       ├── 糯玉米
│       ├── 甜玉米
│       └── 普通玉米
├── 蔬菜
│   ├── 叶菜类
│   │   ├── 白菜
│   │   ├── 菠菜
│   │   └── 油菜
│   ├── 瓜果类
│   │   ├── 黄瓜
│   │   ├── 番茄
│   │   └── 茄子
│   └── 根茎类
│       ├── 白萝卜
│       ├── 胡萝卜
│       └── 马铃薯
├── 水果
│   ├── 仁果类
│   │   ├── 苹果
│   │   │   ├── 红富士
│   │   │   ├── 黄元帅
│   │   │   └── 嘎啦
│   │   └── 梨
│   ├── 核果类
│   │   ├── 桃
│   │   └── 杏
│   └── 浆果类
│       ├── 葡萄
│       └── 草莓
├── 畜禽肉类
│   ├── 猪肉
│   ├── 牛肉
│   └── 羊肉
├── 禽蛋类
│   ├── 鸡蛋
│   └── 鸭蛋
└── 水产品
    ├── 鱼类
    │   ├── 草鱼
    │   └── 鲤鱼
    └── 虾蟹类
        ├── 对虾
        └── 大闸蟹
```

**分类字段定义表**：

| 字段名 | 类型 | 必填 | 说明 |
|:-------|:-----|:-----|:-----|
| 分类ID | VARCHAR | 是 | 系统生成 |
| 分类编码 | VARCHAR | 是 | 层级编码 |
| 分类名称 | VARCHAR | 是 | - |
| 父分类ID | VARCHAR | 否 | - |
| 层级深度 | INT | 是 | - |
| 路径编码 | VARCHAR | 是 | 如：/01/01/001 |
| 排序号 | INT | 否 | - |
| 是否叶子节点 | BOOLEAN | 是 | - |
| 状态 | ENUM | 是 | 启用/停用 |

**分类属性字段定义表**：

| 字段名 | 类型 | 必填 | 说明 |
|:-------|:-----|:-----|:-----|
| 属性编码 | VARCHAR | 是 | - |
| 属性名称 | VARCHAR | 是 | 如：产地 |
| 属性类型 | ENUM | 是 | text/select/number/boolean |
| 是否必填 | BOOLEAN | 是 | - |
| 是否SKU属性 | BOOLEAN | 是 | 影响SKU生成 |
| 可搜索 | BOOLEAN | 否 | - |
| 可筛选 | BOOLEAN | 否 | - |
| 选项值 | JSON | 否 | select类型时使用 |

### 4.4.4 商品标准库页面

**页面路径**：`/master-data/product/standard`

**页面布局**：
- SPU列表
- SPU详情（属性、规格、图片）
- 属性模板配置

**商品编码结构**：

```
商品编码 = 大类码(2位) + 中类码(2位) + 小类码(3位) + 品种码(4位) + 特征码(4位) + 校验码(1位)

示例：
01        01        001       0001       0001       X
粮食作物   稻谷      籼稻      早籼稻     一级      校验码
```

**商品主数据字段定义表**：

| 字段名 | 类型 | 必填 | 说明 |
|:-------|:-----|:-----|:-----|
| 商品ID | VARCHAR | 是 | 系统生成 |
| 商品编码 | VARCHAR | 是 | 唯一编码 |
| 商品名称 | VARCHAR | 是 | - |
| 分类ID | VARCHAR | 是 | 关联分类 |
| 品牌 | VARCHAR | 否 | - |
| 产地 | VARCHAR | 否 | - |
| 计量单位 | VARCHAR | 是 | 如：kg、箱 |
| 参考单价 | DECIMAL | 否 | - |
| 最小起订量 | DECIMAL | 否 | - |
| 状态 | ENUM | 是 | 待审核/已上架/已下架 |

### 4.4.5 核心API接口清单

| 接口名称 | 请求方式 | 路径 | 主要参数 | 返回说明 |
|:---------|:---------|:-----|:---------|:---------|
| 分类树查询 | GET | /api/master-data/products/categories/tree | - | 分类树 |
| 分类详情 | GET | /api/master-data/products/categories/{id} | categoryId | 分类信息 |
| 分类创建 | POST | /api/master-data/products/categories | categoryInfo | 创建结果 |
| 分类更新 | PUT | /api/master-data/products/categories/{id} | categoryId, info | 更新结果 |
| 属性模板查询 | GET | /api/master-data/products/categories/{id}/attributes | categoryId | 属性列表 |
| 属性模板配置 | PUT | /api/master-data/products/categories/{id}/attributes | categoryId, attrs | 配置结果 |
| SPU列表 | GET | /api/master-data/products/spu | page, size, params | SPU列表 |
| SPU详情 | GET | /api/master-data/products/spu/{id} | productId | SPU信息 |
| SPU创建 | POST | /api/master-data/products/spu | productInfo | 创建结果 |
| SPU审核 | PUT | /api/master-data/products/spu/{id}/audit | productId, result | 审核结果 |
| 编码规则查询 | GET | /api/master-data/products/code-rules | entityType | 规则列表 |
| 编码规则配置 | PUT | /api/master-data/products/code-rules | rules | 配置结果 |

### 4.4.6 数据模型设计

#### product_category（商品分类主数据表）

| 字段名 | 字段类型 | 长度 | 说明 | 示例 |
|:-------|:---------|:-----|:-----|:-----|
| category_id | VARCHAR | 32 | 分类ID，主键 | CAT20250100001 |
| category_code | VARCHAR | 50 | 分类编码 | 0101001 |
| category_name | VARCHAR | 100 | 分类名称 | 籼稻 |
| parent_id | VARCHAR | 32 | 父分类ID | CAT20250100000 |
| level | INT | - | 层级深度 | 4 |
| path | VARCHAR | 500 | 路径编码 | /01/01/001/0001 |
| sort_order | INT | - | 排序号 | 1 |
| is_leaf | TINYINT | - | 是否叶子节点 | 1 |
| status | ENUM | - | 状态 | enabled |
| create_time | TIMESTAMP | - | 创建时间 | 2025-01-01 |

#### product_attribute_template（商品属性模板表）

| 字段名 | 字段类型 | 长度 | 说明 | 示例 |
|:-------|:---------|:-----|:-----|:-----|
| template_id | VARCHAR | 32 | 模板ID，主键 | TPL20250100001 |
| template_code | VARCHAR | 50 | 模板编码 | TPL_VEGETABLE |
| template_name | VARCHAR | 100 | 模板名称 | 蔬菜属性模板 |
| category_id | VARCHAR | 32 | 关联分类ID | CAT20250100002 |
| attributes | JSON | - | 属性定义集合 | 见下方示例 |
| status | ENUM | - | 状态 | enabled |
| create_time | TIMESTAMP | - | 创建时间 | 2025-01-01 |

**商品属性定义JSON结构示例**：

```json
{
  "attributes": [
    {
      "attr_code": "origin",
      "attr_name": "产地",
      "attr_type": "text",
      "is_required": true,
      "is_sku_attr": true,
      "searchable": true,
      "filterable": true
    },
    {
      "attr_code": "grade",
      "attr_name": "等级",
      "attr_type": "select",
      "options": ["特级", "一级", "二级", "三级"],
      "is_required": true,
      "is_sku_attr": true,
      "searchable": true,
      "filterable": true
    },
    {
      "attr_code": "spec_weight",
      "attr_name": "规格重量(kg)",
      "attr_type": "number",
      "unit": "kg",
      "is_required": true,
      "is_sku_attr": true
    },
    {
      "attr_code": "organic",
      "attr_name": "有机认证",
      "attr_type": "boolean",
      "is_required": false,
      "is_sku_attr": false
    }
  ]
}
```

#### product_master（商品主数据表）

| 字段名 | 字段类型 | 长度 | 说明 | 示例 |
|:-------|:---------|:-----|:-----|:-----|
| product_id | VARCHAR | 32 | 商品ID，主键 | PRD20250100001 |
| product_code | VARCHAR | 50 | 商品编码 | 010100100010001X |
| product_name | VARCHAR | 200 | 商品名称 | 早籼稻特级 |
| category_id | VARCHAR | 32 | 分类ID | CAT20250100004 |
| brand | VARCHAR | 100 | 品牌 | - |
| origin | VARCHAR | 100 | 产地 | 湖南 |
| unit | VARCHAR | 20 | 计量单位 | kg |
| unit_price | DECIMAL | 10,2 | 参考单价 | 3.50 |
| min_order_qty | DECIMAL | 10,2 | 最小起订量 | 100 |
| status | ENUM | - | 状态 | enabled |
| create_time | TIMESTAMP | - | 创建时间 | 2025-01-01 |

#### product_quality_standard（商品质量标准表）

| 字段名 | 字段类型 | 长度 | 说明 | 示例 |
|:-------|:---------|:-----|:-----|:-----|
| standard_id | VARCHAR | 32 | 标准ID，主键 | STD20250100001 |
| category_id | VARCHAR | 32 | 适用分类 | CAT20250100004 |
| grade_code | VARCHAR | 10 | 等级代码 | A |
| grade_name | VARCHAR | 50 | 等级名称 | 特级 |
| indicators | JSON | - | 质量指标集合 | 见下方示例 |
| check_method | VARCHAR | 200 | 检验方法 | 感官检验 |
| status | ENUM | - | 状态 | enabled |

**质量指标JSON示例**：

```json
{
  "indicators": [
    {
      "name": "外观",
      "requirement": "色泽正常，无病虫害，无机械损伤",
      "check_method": "感官检验"
    },
    {
      "name": "规格",
      "requirement": "单个重量≥200g",
      "check_method": "称重检验",
      "unit": "g",
      "min_value": 200
    },
    {
      "name": "农残限量",
      "requirement": "符合GB 2763规定",
      "check_method": "实验室检测",
      "standard": "GB 2763"
    }
  ]
}
```

### 4.4.7 业务规则与约束

1. **分类层级限制**：分类最多支持6级
2. **叶子节点限制**：有商品的分类不可直接删除
3. **属性继承**：子分类继承父分类属性，可扩展
4. **编码唯一性**：商品编码全平台唯一
5. **审核流程**：新增/修改商品需审核后生效

---

## 4.5 客户供应商主数据

### 4.5.1 域定位与目标

客户供应商主数据管理平台交易双方的完整档案，包括客户档案、供应商档案、资质管理、关系管理等功能，为交易撮合、供应链金融等业务提供信用评估和资质核验基础。

**核心目标**：
- 建立完整的客户供应商档案
- 实现资质认证与有效期管理
- 维护客户供应商关系网络
- 支撑信用评估与分级管理

### 4.5.2 页面清单

| 页面名称 | 路径 | 主要功能 | 用户角色 |
|:---------|:-----|:---------|:---------|
| 客户列表 | /master-data/customer/list | 客户查询、筛选 | 数据管理员 |
| 客户详情 | /master-data/customer/detail/{id} | 客户信息查看、编辑 | 数据管理员 |
| 供应商列表 | /master-data/supplier/list | 供应商查询、筛选 | 数据管理员 |
| 供应商详情 | /master-data/supplier/detail/{id} | 供应商信息查看、编辑 | 数据管理员 |
| 关系管理 | /master-data/relation | 客户供应商关系管理 | 数据管理员 |

### 4.5.3 客户档案管理页面

**页面路径**：`/master-data/customer/list`

**页面布局**：
- 搜索筛选栏
- 客户列表
- 批量操作工具栏

**客户等级枚举**：

| 等级代码 | 等级名称 | 说明 |
|:---------|:---------|:-----|
| AAA | 优质客户 | 交易量大、信用良好 |
| AA | 活跃客户 | 交易稳定 |
| A | 普通客户 | 交易一般 |
| B | 观察客户 | 交易较少 |
| C | 风险客户 | 有逾期或纠纷记录 |

**字段定义表**：

| 字段名 | 类型 | 必填 | 说明 |
|:-------|:-----|:-----|:-----|
| 客户ID | VARCHAR | 是 | 系统生成 |
| 客户编码 | VARCHAR | 是 | - |
| 客户名称 | VARCHAR | 是 | - |
| 客户类型 | ENUM | 是 | 个人/企业 |
| 关联用户ID | VARCHAR | 否 | 关联用户主数据 |
| 关联组织ID | VARCHAR | 否 | 关联组织主数据 |
| 客户等级 | ENUM | 否 | - |
| 信用额度 | DECIMAL | 否 | - |
| 已用额度 | DECIMAL | 否 | - |
| 账期天数 | INT | 否 | - |
| 客户标签 | JSON | 否 | - |
| 客户来源 | VARCHAR | 否 | - |
| 首次下单时间 | TIMESTAMP | 否 | - |
| 最近下单时间 | TIMESTAMP | 否 | - |
| 累计订单金额 | DECIMAL | 否 | - |
| 状态 | ENUM | 是 | - |

### 4.5.4 供应商档案管理页面

**页面路径**：`/master-data/supplier/list`

**页面布局**：
- 搜索筛选栏
- 供应商列表
- 资质管理面板

**字段定义表**：

| 字段名 | 类型 | 必填 | 说明 |
|:-------|:-----|:-----|:-----|
| 供应商ID | VARCHAR | 是 | 系统生成 |
| 供应商编码 | VARCHAR | 是 | - |
| 供应商名称 | VARCHAR | 是 | - |
| 供应商类型 | ENUM | 是 | 个人/企业 |
| 关联用户ID | VARCHAR | 否 | - |
| 关联组织ID | VARCHAR | 否 | - |
| 供应商等级 | ENUM | 否 | - |
| 经营范围 | TEXT | 否 | - |
| 主营产品 | JSON | 否 | - |
| 年供货能力 | DECIMAL | 否 | 万元 |
| 交付周期 | INT | 否 | 天 |
| 最小订单金额 | DECIMAL | 否 | - |
| 资质证书 | JSON | 否 | - |
| 合作状态 | ENUM | 否 | - |
| 首次合作时间 | TIMESTAMP | 否 | - |
| 质量评分 | DECIMAL | 否 | - |
| 交付评分 | DECIMAL | 否 | - |
| 服务评分 | DECIMAL | 否 | - |
| 状态 | ENUM | 是 | - |

### 4.5.5 核心API接口清单

| 接口名称 | 请求方式 | 路径 | 主要参数 | 返回说明 |
|:---------|:---------|:-----|:---------|:---------|
| 客户列表查询 | GET | /api/master-data/customers | page, size, params | 客户列表 |
| 客户详情查询 | GET | /api/master-data/customers/{id} | customerId | 客户详情 |
| 客户创建 | POST | /api/master-data/customers | customerInfo | 创建结果 |
| 客户更新 | PUT | /api/master-data/customers/{id} | customerId, info | 更新结果 |
| 供应商列表查询 | GET | /api/master-data/suppliers | page, size, params | 供应商列表 |
| 供应商详情查询 | GET | /api/master-data/suppliers/{id} | supplierId | 供应商详情 |
| 供应商创建 | POST | /api/master-data/suppliers | supplierInfo | 创建结果 |
| 供应商更新 | PUT | /api/master-data/suppliers/{id} | supplierId, info | 更新结果 |
| 关系列表查询 | GET | /api/master-data/relations | customerId/supplierId | 关系列表 |
| 关系创建 | POST | /api/master-data/relations | relationInfo | 创建结果 |

### 4.5.6 数据模型设计

#### customer_master（客户主数据表）

| 字段名 | 字段类型 | 长度 | 说明 | 示例 |
|:-------|:---------|:-----|:-----|:-----|
| customer_id | VARCHAR | 32 | 客户ID，主键 | CST20250100001 |
| customer_code | VARCHAR | 50 | 客户编码 | CST20250100001 |
| customer_name | VARCHAR | 200 | 客户名称 | xx贸易公司 |
| customer_type | ENUM | - | 客户类型 | enterprise |
| user_id | VARCHAR | 32 | 关联用户ID | USR20250100001 |
| org_id | VARCHAR | 32 | 关联组织ID | ORG20250100001 |
| customer_level | ENUM | - | 客户等级 | AAA |
| credit_limit | DECIMAL | 15,2 | 信用额度 | 100000.00 |
| used_credit | DECIMAL | 15,2 | 已用额度 | 30000.00 |
| payment_days | INT | - | 账期天数 | 30 |
| tags | JSON | - | 客户标签 | ["优质客户","长期合作"] |
| source_channel | VARCHAR | 50 | 客户来源 | 线上推广 |
| first_order_time | TIMESTAMP | - | 首次下单时间 | 2024-06-01 |
| last_order_time | TIMESTAMP | - | 最近下单时间 | 2025-01-15 |
| total_order_amount | DECIMAL | 15,2 | 累计订单金额 | 500000.00 |
| status | ENUM | - | 状态 | active |
| create_time | TIMESTAMP | - | 创建时间 | 2024-01-01 |

#### supplier_master（供应商主数据表）

| 字段名 | 字段类型 | 长度 | 说明 | 示例 |
|:-------|:---------|:-----|:-----|:-----|
| supplier_id | VARCHAR | 32 | 供应商ID，主键 | SUP20250100001 |
| supplier_code | VARCHAR | 50 | 供应商编码 | SUP20250100001 |
| supplier_name | VARCHAR | 200 | 供应商名称 | xx农场 |
| supplier_type | ENUM | - | 供应商类型 | enterprise |
| user_id | VARCHAR | 32 | 关联用户ID | USR20250100002 |
| org_id | VARCHAR | 32 | 关联组织ID | ORG20250100002 |
| supplier_level | ENUM | - | 供应商等级 | AA |
| business_scope | TEXT | - | 经营范围 | 农产品种植、销售 |
| main_products | JSON | - | 主营产品 | ["番茄","黄瓜"] |
| annual_capacity | DECIMAL | 15,2 | 年供货能力 | 500.00 |
| delivery_days | INT | - | 交付周期(天) | 3 |
| min_order_amount | DECIMAL | 15,2 | 最小订单金额 | 1000.00 |
| certifications | JSON | - | 资质证书列表 | [...} |
| cooperation_status | ENUM | - | 合作状态 | active |
| first_coop_time | TIMESTAMP | - | 首次合作时间 | 2024-01-01 |
| total_purchase_amount | DECIMAL | 15,2 | 累计采购金额 | 800000.00 |
| quality_score | DECIMAL | 3,2 | 质量评分 | 4.50 |
| delivery_score | DECIMAL | 3,2 | 交付评分 | 4.80 |
| service_score | DECIMAL | 3,2 | 服务评分 | 4.60 |
| status | ENUM | - | 状态 | active |
| create_time | TIMESTAMP | - | 创建时间 | 2024-01-01 |

#### customer_supplier_relation（客户供应商关系表）

| 字段名 | 字段类型 | 长度 | 说明 | 示例 |
|:-------|:---------|:-----|:-----|:-----|
| relation_id | VARCHAR | 32 | 关系ID，主键 | REL20250100001 |
| customer_id | VARCHAR | 32 | 客户ID | CST20250100001 |
| supplier_id | VARCHAR | 32 | 供应商ID | SUP20250100001 |
| relation_type | ENUM | - | 关系类型 | supplier |
| cooperation_level | ENUM | - | 合作程度 | stable |
| start_date | DATE | - | 合作开始日期 | 2024-01-01 |
| end_date | DATE | - | 合作结束日期 | - |
| transaction_count | INT | - | 交易次数 | 50 |
| total_amount | DECIMAL | 15,2 | 累计交易额 | 200000.00 |
| avg_unit_price | DECIMAL | 10,2 | 平均单价 | 4.00 |
| payment_status | ENUM | - | 付款状态 | normal |
| dispute_count | INT | - | 纠纷次数 | 0 |
| status | ENUM | - | 关系状态 | active |
| create_time | TIMESTAMP | - | 创建时间 | 2024-01-01 |

### 4.5.7 业务规则与约束

1. **关联约束**：客户/供应商必须关联用户或组织
2. **等级更新**：交易数据触发等级自动评估
3. **资质有效期**：资质过期前15天提醒
4. **关系唯一性**：同一客户供应商同一类型关系唯一

---

## 4.6 地址主数据

### 4.6.1 域定位与目标

地址主数据管理行政区划、产区、物流节点等地理信息，为平台提供标准化的地址数据基础，支撑农产品产地溯源、物流跟踪等业务场景。

**核心目标**：
- 建立标准化的行政区划数据
- 管理农产品产区信息
- 维护物流节点数据
- 实现地址标准化与验证

### 4.6.2 页面清单

| 页面名称 | 路径 | 主要功能 | 用户角色 |
|:---------|:-----|:---------|:---------|
| 行政区划 | /master-data/address/region | 省市区三级联动、编码管理 | 数据管理员 |
| 产区管理 | /master-data/address/production-region | 产区主数据管理 | 数据管理员 |
| 物流节点 | /master-data/address/logistics-node | 仓储、物流节点管理 | 数据管理员 |
| 地址标准化 | /master-data/address/standardization | 地址清洗、补全、验证 | 数据管理员 |

### 4.6.3 行政区划管理页面

**页面路径**：`/master-data/address/region`

**页面布局**：
- 左侧：行政区划树
- 右侧：选中区域详情

**行政级别说明**：

| 级别代码 | 级别名称 | 说明 |
|:---------|:---------|:-----|
| 1 | 省级 | 省、自治区、直辖市 |
| 2 | 市级 | 地级市、地区、自治州 |
| 3 | 区县 | 区、县、县级市 |
| 4 | 乡镇级 | 镇、乡、街道 |
| 5 | 村级 | 村、社区 |

**字段定义表**：

| 字段名 | 类型 | 必填 | 说明 |
|:-------|:-----|:-----|:-----|
| 区域ID | VARCHAR | 是 | 系统生成 |
| 区域编码 | VARCHAR | 是 | 国标编码 |
| 区域名称 | VARCHAR | 是 | - |
| 父区域编码 | VARCHAR | 否 | - |
| 行政级别 | INT | 是 | 1-5 |
| 拼音 | VARCHAR | 否 | - |
| 拼音缩写 | VARCHAR | 否 | - |
| 中心点纬度 | DECIMAL | 否 | - |
| 中心点经度 | DECIMAL | 否 | - |
| 长途区号 | VARCHAR | 否 | - |
| 邮政编码 | VARCHAR | 否 | - |
| 状态 | ENUM | 是 | - |

### 4.6.4 核心API接口清单

| 接口名称 | 请求方式 | 路径 | 主要参数 | 返回说明 |
|:---------|:---------|:-----|:---------|:---------|
| 行政区划树 | GET | /api/master-data/address/regions/tree | parentCode | 树形结构 |
| 行政区划详情 | GET | /api/master-data/address/regions/{code} | regionCode | 区域详情 |
| 子区域查询 | GET | /api/master-data/address/regions/{code}/children | regionCode | 子区域列表 |
| 地址标准化 | POST | /api/master-data/address/standardize | rawAddress | 标准化结果 |
| 地址补全 | POST | /api/master-data/address/complete | partialAddress | 补全建议 |
| 地址验证 | POST | /api/master-data/address/validate | address | 验证结果 |
| 产区列表 | GET | /api/master-data/address/production-regions | params | 产区列表 |
| 物流节点列表 | GET | /api/master-data/address/logistics-nodes | params | 节点列表 |

### 4.6.5 数据模型设计

#### address_region（行政区划表）

| 字段名 | 字段类型 | 长度 | 说明 | 示例 |
|:-------|:---------|:-----|:-----|:-----|
| region_id | VARCHAR | 32 | 区域ID，主键 | REG20250100001 |
| region_code | VARCHAR | 20 | 区域编码(国标) | 110000 |
| region_name | VARCHAR | 100 | 区域名称 | 北京市 |
| parent_code | VARCHAR | 20 | 父区域编码 | 100000 |
| level | INT | - | 行政级别：1省/2市/3区县/4乡镇/5村 | 1 |
| pinyin | VARCHAR | 100 | 拼音 | bei jing shi |
| pinyin_abbr | VARCHAR | 20 | 拼音缩写 | BJS |
| latitude | DECIMAL | 10,7 | 中心点纬度 | 39.904211 |
| longitude | DECIMAL | 10,7 | 中心点经度 | 116.407397 |
| area_code | VARCHAR | 10 | 长途区号 | 010 |
| postal_code | VARCHAR | 10 | 邮政编码 | 100000 |
| status | ENUM | - | 状态 | enabled |
| create_time | TIMESTAMP | - | 创建时间 | 2025-01-01 |

#### product_region（产区主数据表）

| 字段名 | 字段类型 | 长度 | 说明 | 示例 |
|:-------|:---------|:-----|:-----|:-----|
| region_id | VARCHAR | 32 | 产区ID，主键 | PRG20250100001 |
| region_code | VARCHAR | 50 | 产区编码 | PRG_YCH_DZX |
| region_name | VARCHAR | 100 | 产区名称 | 阳澄湖大闸蟹 |
| region_type | ENUM | - | 产区类型 | geographical_indication |
| main_products | JSON | - | 主要产品 | ["大闸蟹"] |
| description | TEXT | - | 产区描述 | - |
| advantage | VARCHAR | 500 | 产区优势 | - |
| capacity | DECIMAL | 15,2 | 年产能 | 2000.00 |
| output_ratio | DECIMAL | 5,2 | 全国占比(%) | 30.00 |
| status | ENUM | - | 状态 | enabled |

#### wholesale_market（批发市场表）

| 字段名 | 字段类型 | 长度 | 说明 | 示例 |
|:-------|:---------|:-----|:-----|:-----|
| market_id | VARCHAR | 32 | 市场ID，主键 | MKT20250100001 |
| market_code | VARCHAR | 50 | 市场编码 | MKT_SG_VEG |
| market_name | VARCHAR | 200 | 市场名称 | 寿光蔬菜批发市场 |
| market_type | ENUM | - | 市场类型 | wholesale |
| region_code | VARCHAR | 20 | 所属行政区划 | 370700 |
| address | VARCHAR | 200 | 市场地址 | - |
| longitude | DECIMAL | 10,7 | 经度 | 118.790000 |
| latitude | DECIMAL | 10,7 | 纬度 | 36.850000 |
| business_area | DECIMAL | 10,2 | 营业面积(㎡) | 50000.00 |
| booth_count | INT | - | 摊位数量 | 2000 |
| main_products | JSON | - | 主营品类 | ["蔬菜","水果"] |
| business_hours | VARCHAR | 50 | 营业时间 | 0:00-18:00 |
| manager_name | VARCHAR | 50 | 负责人 | - |
| manager_phone | VARCHAR | 20 | 联系电话 | - |
| status | ENUM | - | 状态 | enabled |

#### logistics_node（物流节点表）

| 字段名 | 字段类型 | 长度 | 说明 | 示例 |
|:-------|:---------|:-----|:-----|:-----|
| node_id | VARCHAR | 32 | 节点ID，主键 | LND20250100001 |
| node_code | VARCHAR | 50 | 节点编码 | LND_BJ_WH |
| node_name | VARCHAR | 200 | 节点名称 | 北京中心仓 |
| node_type | ENUM | - | 节点类型 | warehouse |
| region_code | VARCHAR | 20 | 所属区域 | 110000 |
| address | VARCHAR | 200 | 详细地址 | - |
| longitude | DECIMAL | 10,7 | 经度 | 116.407397 |
| latitude | DECIMAL | 10,7 | 纬度 | 39.904211 |
| capacity | JSON | - | 容量配置 | {...} |
| services | JSON | - | 服务能力 | [...} |
| contact_person | VARCHAR | 50 | 联系人 | - |
| contact_phone | VARCHAR | 20 | 联系电话 | - |
| status | ENUM | - | 状态 | enabled |

### 4.6.6 业务规则与约束

1. **国标编码**：行政区划编码遵循GB/T 2260标准
2. **层级一致性**：子区域编码必须以父区域编码开头
3. **唯一性**：同一级别区域编码唯一
4. **历史数据**：区域调整时保留历史数据

---

## 4.7 数据标准管理

### 4.7.1 域定位与目标

数据标准管理建立平台统一的数据标准体系，包括数据字典、编码规则、格式标准、命名规范等，为主数据质量和跨系统数据一致性提供保障。

**核心目标**：
- 建立统一的数据字典
- 制定数据编码规则
- 规范数据格式标准
- 实现标准对标检测

### 4.7.2 页面清单

| 页面名称 | 路径 | 主要功能 | 用户角色 |
|:---------|:-----|:---------|:---------|
| 数据字典 | /master-data/standard/dictionary | 字典项管理 | 数据管理员 |
| 编码规则 | /master-data/standard/code-rule | 编码规则配置 | 系统管理员 |
| 格式标准 | /master-data/standard/format | 数据格式规范 | 数据管理员 |
| 命名规范 | /master-data/standard/naming | 命名规则管理 | 数据管理员 |
| 对标检测 | /master-data/standard/check | 标准一致性检测 | 数据管理员 |

### 4.7.3 数据字典管理页面

**页面路径**：`/master-data/standard/dictionary`

**页面布局**：
- 左侧：字典分类树
- 右侧：字典项列表

**字段定义表**：

| 字段名 | 类型 | 必填 | 说明 |
|:-------|:-----|:-----|:-----|
| 字典ID | VARCHAR | 是 | - |
| 字典编码 | VARCHAR | 是 | 唯一标识 |
| 字典名称 | VARCHAR | 是 | - |
| 字典类型 | ENUM | 是 | system/business |
| 父字典ID | VARCHAR | 否 | - |
| 字典项 | JSON | 否 | - |
| 描述 | VARCHAR | 否 | - |
| 状态 | ENUM | 是 | - |

### 4.7.4 核心API接口清单

| 接口名称 | 请求方式 | 路径 | 主要参数 | 返回说明 |
|:---------|:---------|:-----|:---------|:---------|
| 字典树查询 | GET | /api/v1/masterdata/standards/dictionaries/tree | - | 字典树 |
| 字典详情 | GET | /api/v1/masterdata/standards/dictionaries/{id} | dictId | 字典信息 |
| 字典创建 | POST | /api/v1/masterdata/standards/dictionaries | dictInfo | 创建结果 |
| 字典更新 | PUT | /api/v1/masterdata/standards/dictionaries/{id} | dictId, info | 更新结果 |
| 编码规则查询 | GET | /api/v1/masterdata/standards/code-rules | entityType | 规则列表 |
| 编码规则配置 | PUT | /api/v1/masterdata/standards/code-rules | rules | 配置结果 |
| 标准对标检测 | POST | /api/v1/masterdata/standards/check | entityType, dataIds | 检测结果 |

### 4.7.5 数据模型设计

#### data_dictionary（数据字典表）

| 字段名 | 字段类型 | 长度 | 说明 | 示例 |
|:-------|:---------|:-----|:-----|:-----|
| dict_id | VARCHAR | 32 | 字典ID，主键 | DICT20250100001 |
| dict_code | VARCHAR | 50 | 字典编码 | user_status |
| dict_name | VARCHAR | 100 | 字典名称 | 用户状态 |
| dict_type | ENUM | - | 字典类型 | system |
| parent_id | VARCHAR | 32 | 父字典ID | - |
| items | JSON | - | 字典项集合 | 见下方示例 |
| description | VARCHAR | 500 | 描述 | - |
| status | ENUM | - | 状态 | enabled |
| create_time | TIMESTAMP | - | 创建时间 | 2025-01-01 |

**字典项JSON结构**：

```json
{
  "items": [
    {
      "item_code": "active",
      "item_name": "正常",
      "item_value": "1",
      "sort_order": 1,
      "is_default": true
    },
    {
      "item_code": "frozen",
      "item_name": "冻结",
      "item_value": "2",
      "sort_order": 2,
      "is_default": false
    },
    {
      "item_code": "cancelled",
      "item_name": "注销",
      "item_value": "3",
      "sort_order": 3,
      "is_default": false
    }
  ]
}
```

#### data_code_rule（数据编码规则表）

| 字段名 | 字段类型 | 长度 | 说明 | 示例 |
|:-------|:---------|:-----|:-----|:-----|
| rule_id | VARCHAR | 32 | 规则ID，主键 | RULE20250100001 |
| entity_type | VARCHAR | 50 | 实体类型 | user |
| rule_name | VARCHAR | 100 | 规则名称 | 用户编码规则 |
| rule_pattern | VARCHAR | 200 | 编码模式 | USR+yyyyMMdd+6位序号 |
| prefix | VARCHAR | 50 | 前缀 | USR |
| date_format | VARCHAR | 50 | 日期格式 | yyyyMMdd |
| sequence_start | BIGINT | - | 序列起始值 | 1 |
| sequence_step | INT | - | 序列步长 | 1 |
| sequence_length | INT | - | 序列位数 | 6 |
| suffix | VARCHAR | 50 | 后缀 | - |
| example | VARCHAR | 100 | 示例 | USR20250115000001 |

**编码模式示例**：

| 实体类型 | 编码模式 | 示例 |
|:---------|:---------|:-----|
| 用户 | USR+yyyyMMdd+6位序号 | USR20250115000001 |
| 组织 | ORG+yyyyMMdd+6位序号 | ORG20250115000001 |
| 订单 | ORD+yyyyMMddHHmmss+4位序号 | ORD202501151000000001 |
| 商品 | 大类+中类+小类+品种+特征+校验码 | 010100100010001X |

#### data_format_standard（数据格式标准表）

| 字段名 | 字段类型 | 长度 | 说明 | 示例 |
|:-------|:---------|:-----|:-----|:-----|
| standard_id | VARCHAR | 32 | 标准ID，主键 | FMT20250100001 |
| data_type | VARCHAR | 50 | 数据类型 | user |
| field_name | VARCHAR | 100 | 字段名称 | phone |
| data_format | VARCHAR | 100 | 数据格式 | ^1[3-9]\d{9}$ |
| max_length | INT | - | 最大长度 | 11 |
| precision | INT | - | 精度 | - |
| required | TINYINT | - | 是否必填 | 1 |
| default_value | VARCHAR | 100 | 默认值 | - |
| validation_rule | VARCHAR | 200 | 校验规则 | 手机号格式 |
| description | VARCHAR | 500 | 描述 | 中国大陆手机号 |

### 4.7.6 业务规则与约束

1. **字典唯一性**：字典编码全平台唯一
2. **编码唯一性**：同一实体类型编码规则唯一
3. **版本管理**：标准变更需版本控制
4. **影响分析**：修改标准前需评估影响范围

---

## 4.8 数据质量管理

### 4.8.1 域定位与目标

数据质量管理建立主数据质量的监控和改进体系，覆盖完整性、准确性、一致性、时效性四个维度，确保主数据资产的高可用性。

**核心目标**：
- 建立多维度的质量规则体系
- 实现质量问题的闭环处理
- 提供质量状况的可视化展示
- 持续提升主数据质量

### 4.8.2 页面清单

| 页面名称 | 路径 | 主要功能 | 用户角色 |
|:---------|:-----|:---------|:---------|
| 质量规则 | /master-data/quality/rules | 规则配置与管理 | 数据管理员 |
| 检测任务 | /master-data/quality/tasks | 任务调度与执行 | 数据管理员 |
| 问题工单 | /master-data/quality/issues | 问题跟踪处理 | 数据管理员 |
| 质量仪表盘 | /master-data/quality/dashboard | 质量评分与趋势 | 数据管理员、管理层 |

### 4.8.3 质量规则配置页面

**页面路径**：`/master-data/quality/rules`

**页面布局**：
- 规则列表
- 新增/编辑规则表单

**规则类型枚举**：

| 规则类型 | 说明 | 示例 |
|:---------|:-----|:-----|
| completeness | 完整性 | 非空检查、必填项检查 |
| accuracy | 准确性 | 格式校验、范围校验 |
| consistency | 一致性 | 跨表一致性、同步检查 |
| timeliness | 时效性 | 数据更新时间检查 |
| uniqueness | 唯一性 | 主键唯一、编码唯一 |
| validity | 有效性 | 枚举值检查、关联检查 |

**检查维度枚举**：

| 维度代码 | 维度名称 | 说明 |
|:---------|:---------|:-----|
| NULL_CHECK | 空值检查 | 字段是否为空 |
| RANGE_CHECK | 范围检查 | 数值是否在合理范围 |
| FORMAT_CHECK | 格式检查 | 格式是否符合规范 |
| LOGIC_CHECK | 逻辑检查 | 逻辑关系是否正确 |
| REFERENCE_CHECK | 引用检查 | 外键是否有效 |
| DUPLICATE_CHECK | 重复检查 | 是否存在重复记录 |

**字段定义表**：

| 字段名 | 类型 | 必填 | 说明 |
|:-------|:-----|:-----|:-----|
| 规则ID | VARCHAR | 是 | - |
| 规则编码 | VARCHAR | 是 | - |
| 规则名称 | VARCHAR | 是 | - |
| 规则类型 | ENUM | 是 | - |
| 实体类型 | VARCHAR | 是 | - |
| 检查字段 | VARCHAR | 是 | - |
| 检查维度 | ENUM | 是 | - |
| 检查表达式 | TEXT | 是 | SQL或规则表达式 |
| 阈值 | VARCHAR | 否 | - |
| 预警级别 | ENUM | 是 | 低/中/高/严重 |
| 状态 | ENUM | 是 | - |

### 4.8.4 质量仪表盘

**页面路径**：`/masterdata/quality/dashboard`

**页面布局**：
- 顶部：质量总览卡片（评分、通过率、问题数）
- 中部：质量趋势图（折线图）
- 底部：问题分布图（饼图/柱状图）

**质量评分计算**：

```
质量评分 = (通过记录数 / 总记录数) × 100

预警级别权重：
- 低：扣1分
- 中：扣3分
- 高：扣5分
- 严重：扣10分
```

### 4.8.5 核心API接口清单

| 接口名称 | 请求方式 | 路径 | 主要参数 | 返回说明 |
|:---------|:---------|:-----|:---------|:---------|
| 质量规则列表 | GET | /api/master-data/quality/rules | params | 规则列表 |
| 质量规则详情 | GET | /api/master-data/quality/rules/{id} | ruleId | 规则信息 |
| 质量规则创建 | POST | /api/master-data/quality/rules | ruleInfo | 创建结果 |
| 质量规则更新 | PUT | /api/master-data/quality/rules/{id} | ruleId, info | 更新结果 |
| 质量检测执行 | POST | /api/master-data/quality/check | ruleId/entityType | 检测结果 |
| 检测任务创建 | POST | /api/master-data/quality/tasks | taskInfo | 创建结果 |
| 检测任务列表 | GET | /api/master-data/quality/tasks | params | 任务列表 |
| 问题工单列表 | GET | /api/master-data/quality/issues | params | 工单列表 |
| 问题工单详情 | GET | /api/master-data/quality/issues/{id} | issueId | 工单详情 |
| 问题处理 | PUT | /api/master-data/quality/issues/{id}/resolve | issueId, info | 处理结果 |
| 质量仪表盘 | GET | /api/master-data/quality/dashboard | dateRange | 统计数据 |

### 4.8.6 数据模型设计

#### data_quality_rule（数据质量规则表）

| 字段名 | 字段类型 | 长度 | 说明 | 示例 |
|:-------|:---------|:-----|:-----|:-----|
| rule_id | VARCHAR | 32 | 规则ID，主键 | QR20250100001 |
| rule_code | VARCHAR | 50 | 规则编码 | USER_PHONE_NULL |
| rule_name | VARCHAR | 100 | 规则名称 | 用户手机号非空检查 |
| rule_type | ENUM | - | 规则类型 | completeness |
| entity_type | VARCHAR | 50 | 实体类型 | user |
| field_name | VARCHAR | 100 | 检查字段 | phone |
| check_dimension | ENUM | - | 检查维度 | NULL_CHECK |
| check_expression | TEXT | - | 检查表达式 | phone IS NOT NULL |
| threshold_value | VARCHAR | 100 | 阈值 | 100% |
| alert_level | ENUM | - | 预警级别 | high |
| status | ENUM | - | 状态 | enabled |
| create_time | TIMESTAMP | - | 创建时间 | 2025-01-01 |

#### data_quality_result（数据质量检测结果表）

| 字段名 | 字段类型 | 长度 | 说明 | 示例 |
|:-------|:---------|:-----|:-----|:-----|
| result_id | VARCHAR | 32 | 结果ID，主键 | QR20250100001 |
| rule_id | VARCHAR | 32 | 规则ID | QR20250100001 |
| check_time | TIMESTAMP | - | 检测时间 | 2025-01-15 10:00:00 |
| total_count | BIGINT | - | 总记录数 | 10000 |
| pass_count | BIGINT | - | 通过数 | 9850 |
| fail_count | BIGINT | - | 失败数 | 150 |
| pass_rate | DECIMAL | 5,2 | 通过率 | 98.50 |
| fail_records | JSON | - | 失败记录样本 | [...] |
| quality_score | DECIMAL | 5,2 | 质量评分 | 98.50 |
| status | ENUM | - | 状态 | completed |

#### data_issue（数据问题表）

| 字段名 | 字段类型 | 长度 | 说明 | 示例 |
|:-------|:---------|:-----|:-----|:-----|
| issue_id | VARCHAR | 32 | 问题ID，主键 | ISS20250100001 |
| issue_code | VARCHAR | 50 | 问题编码 | ISS-2025-00001 |
| issue_type | ENUM | - | 问题类型 | completeness |
| entity_type | VARCHAR | 50 | 实体类型 | user |
| entity_id | VARCHAR | 32 | 记录ID | USR20250100001 |
| field_name | VARCHAR | 100 | 问题字段 | phone |
| issue_desc | TEXT | - | 问题描述 | 手机号为空 |
| issue_level | ENUM | - | 问题级别 | medium |
| issue_status | ENUM | - | 处理状态 | open |
| assignee | VARCHAR | 32 | 处理人 | admin |
| assignee_time | TIMESTAMP | - | 分配时间 | 2025-01-15 |
| resolve_time | TIMESTAMP | - | 解决时间 | - |
| resolve_method | VARCHAR | 200 | 解决方法 | - |
| source_system | VARCHAR | 50 | 数据来源 | master_data |
| create_time | TIMESTAMP | - | 创建时间 | 2025-01-15 |

### 4.8.7 业务规则与约束

1. **规则优先级**：按预警级别排序处理
2. **自动处理**：低级别问题可自动修复
3. **升级机制**：未处理问题自动升级
4. **闭环管理**：所有问题需记录处理结果
5. **定期检测**：每日凌晨执行全量质量检测

---

## 数据安全与合规设计

### 5.1 敏感数据加密

#### 5.1.1 加密字段清单

| 字段类型 | 字段示例 | 加密方式 | 说明 |
|:---------|:---------|:---------|:-----|
| 证件号码 | id_card_no | AES-256加密 | 身份证、营业执照 |
| 手机号码 | phone | 脱敏存储 | 显示时脱敏 |
| 银行卡号 | bank_card_no | AES-256加密 | - |
| 密码 | password | bcrypt哈希 | 不可逆加密 |
| 邮箱 | email | 可选加密 | - |

#### 5.1.2 字段级加密实现

```sql
-- 证件号码加密存储示例
id_card_no VARCHAR(50) COMMENT '证件号码，加密存储'

-- 加密算法：AES-256-CBC
-- 密钥管理：使用KMS服务管理密钥
-- 解密权限：仅限授权接口访问
```

### 5.2 数据访问审计

#### 5.2.1 审计日志内容

| 字段 | 说明 |
|:-----|:-----|
| 操作时间 | 访问时间戳 |
| 用户ID | 操作用户 |
| 操作类型 | 查询/修改/删除/导出 |
| 访问对象 | 数据类型和ID |
| 访问字段 | 涉及的字段 |
| 访问结果 | 成功/失败 |
| 客户端IP | 请求来源IP |

#### 5.2.2 审计留存周期

| 数据类型 | 留存周期 |
|:---------|:---------|
| 操作日志 | 1年 |
| 变更记录 | 3年 |
| 敏感数据访问 | 3年 |
| 数据导出记录 | 永久 |

### 5.3 数据变更审计

#### 5.3.1 变更记录内容

| 字段 | 说明 |
|:-----|:-----|
| 变更ID | 唯一标识 |
| 变更类型 | CREATE/UPDATE/DELETE |
| 变更前数据 | JSON格式 |
| 变更后数据 | JSON格式 |
| 变更差异 | diff展示 |
| 变更原因 | - |
| 操作人 | - |
| 操作时间 | - |

### 5.4 数据备份与恢复

#### 5.4.1 备份策略

| 备份类型 | 频率 | 保留周期 | 存储位置 |
|:---------|:-----|:---------|:---------|
| 全量备份 | 每周 | 4周 | 冷存储 |
| 增量备份 | 每日 | 7天 | 热存储 |
| 实时备份 | 实时 | 30天 | 异地容灾 |

#### 5.4.2 恢复指标

| 指标 | 目标值 |
|:-----|:-------|
| RPO（恢复点目标） | <1小时 |
| RTO（恢复时间目标） | <4小时 |

### 5.5 GDPR/个保法合规要求

1. **数据最小化**：仅收集业务必需的数据
2. **告知同意**：用户知情并同意数据处理
3. **访问权限**：用户可查询自己的数据
4. **删除权利**：用户可请求删除个人数据
5. **数据可携**：支持数据导出
6. **匿名化处理**：统计分析使用匿名化数据

---

## 非功能性需求

### 6.1 性能要求

| 指标 | 要求 | 说明 |
|:-----|:-----|:-----|
| 单条查询响应时间(P99) | <200ms | 主数据查询接口 |
| 列表查询响应时间(P99) | <500ms | 分页列表接口 |
| 批量导入性能 | >1000条/秒 | 批量数据导入 |
| 并发写入 | >500 TPS | 数据写入接口 |
| 分发延迟 | <5秒 | 主数据变更同步延迟 |

### 6.2 安全要求

| 指标 | 要求 |
|:-----|:-----|
| 数据加密 | 敏感字段100%加密存储 |
| 访问控制 | 基于RBAC的权限控制 |
| 审计日志 | 100%操作留痕 |
| API鉴权 | OAuth2.0认证 |

### 6.3 可用性要求

| 指标 | 要求 |
|:-----|:-----|
| 服务可用性 | >99.9% |
| 单点故障 | 无 |
| 降级策略 | 支持服务降级 |
| 熔断机制 | 支持接口熔断 |

### 6.4 兼容性要求

| 维度 | 要求 |
|:-----|:-----|
| PC端 | Chrome/Firefox/Edge/Safari最新版 |
| 移动端 | H5适配 |
| API版本 | 支持多版本共存 |

### 6.5 数据一致性要求

| 维度 | 要求 |
|:-----|:-----|
| 分布式事务 | 最终一致性 |
| 数据同步 | 准实时同步 |
| 冲突处理 | 以主数据为准 |
| 补偿机制 | 失败重试+告警 |

---

## 运营管理系统

### 7.1 运营角色与权限矩阵

| 角色 | 权限范围 |
|:-----|:---------|
| 超级管理员 | 全部功能 |
| 主数据管理员 | 全部主数据域的增删改查 |
| 数据标准管理员 | 数据标准管理全部功能 |
| 数据质量管理员 | 数据质量管理全部功能 |
| 审计员 | 仅查看审计日志 |
| 业务模块管理员 | 仅查看和订阅主数据 |

### 7.2 核心运营页面

#### 7.2.1 主数据总览页面

**页面路径**：`/master-data/operation/overview`

**页面布局**：
- 顶部：统计卡片（用户总数、组织总数、商品总数等）
- 中部：各域数据量分布图
- 底部：最近变更列表

#### 7.2.2 变更审计页面

**页面路径**：`/master-data/operation/audit`

**页面布局**：
- 筛选条件（时间范围、操作类型、操作人）
- 变更记录列表
- 变更详情弹窗

#### 7.2.3 系统配置页面

**页面路径**：`/master-data/operation/config`

**页面布局**：
- 分组配置项
- 配置值编辑表单

### 7.3 运营工作台

**页面路径**：`/masterdata/operation/workbench`

**页面布局**：
- 待办事项卡片
- 今日数据统计
- 快捷操作入口
- 异常告警列表

---

## 数据字典

### 8.1 状态枚举定义

#### 用户状态（user_status）

| 枚举值 | 显示名称 | 说明 |
|:-------|:---------|:-----|
| active | 正常 | 正常状态，可正常使用 |
| frozen | 冻结 | 账号冻结，暂停使用 |
| cancelled | 注销 | 账号注销，数据保留 |

#### 实名认证状态（real_name_status）

| 枚举值 | 显示名称 | 说明 |
|:-------|:---------|:-----|
| none | 未认证 | 尚未提交认证 |
| pending | 认证中 | 认证审核中 |
| verified | 已认证 | 认证通过 |
| rejected | 认证失败 | 认证未通过 |

#### 组织状态（org_status）

| 枚举值 | 显示名称 | 说明 |
|:-------|:---------|:-----|
| active | 正常 | 正常运营 |
| suspended | 停业 | 暂停运营 |
| cancelled | 注销 | 已注销 |

#### 商品状态（status）

| 枚举值 | 显示名称 | 说明 |
|:-------|:---------|:-----|
| draft | 草稿 | 待提交审核 |
| pending | 待审核 | 审核中 |
| approved | 已审核 | 审核通过 |
| rejected | 已拒绝 | 审核未通过 |
| enabled | 已上架 | 可正常使用 |
| disabled | 已下架 | 暂停使用 |

#### 质量检测状态（issue_status）

| 枚举值 | 显示名称 | 说明 |
|:-------|:---------|:-----|
| open | 待处理 | 问题待处理 |
| assigned | 已分配 | 已分配处理人 |
| processing | 处理中 | 处理中 |
| resolved | 已解决 | 问题已解决 |
| verified | 已验证 | 验证通过 |
| closed | 已关闭 | 流程关闭 |

#### 同步状态（sync_status）

| 枚举值 | 显示名称 | 说明 |
|:-------|:---------|:-----|
| pending | 等待中 | 等待同步 |
| syncing | 同步中 | 同步执行中 |
| success | 同步成功 | 同步完成 |
| failed | 同步失败 | 同步失败 |

### 8.2 错误码定义

| 错误码 | 错误信息 | 说明 |
|:-------|:---------|:-----|
| MD-001 | 用户不存在 | 指定的用户ID不存在 |
| MD-002 | 手机号已注册 | 手机号已被其他用户使用 |
| MD-003 | 组织不存在 | 指定的组织ID不存在 |
| MD-004 | 角色不存在 | 指定的角色ID不存在 |
| MD-005 | 分类不存在 | 指定的分类ID不存在 |
| MD-006 | 商品不存在 | 指定的商品ID不存在 |
| MD-007 | 权限不足 | 当前用户无此操作权限 |
| MD-008 | 数据已引用 | 数据被其他记录引用，无法删除 |
| MD-009 | 编码重复 | 编码已存在 |
| MD-010 | 审核状态不允许操作 | 当前状态不允许此操作 |

### 8.3 编码规则枚举

#### 用户编码规则

```
格式：USR+yyyyMMdd+6位序号
示例：USR20250115000001
```

#### 组织编码规则

```
格式：ORG+yyyyMMdd+6位序号
示例：ORG20250115000001
```

#### 商品编码规则

```
格式：大类(2位)+中类(2位)+小类(3位)+品种(4位)+特征(4位)+校验码(1位)
示例：010100100010001X
```

#### 地址编码规则

```
格式：遵循GB/T 2260国标编码
示例：110000（北京市）
```

---

## 核心业务流程设计

### 9.1 主数据创建→审核→发布→分发全流程

```
┌─────────────────────────────────────────────────────────────────────────────┐
│                        主数据创建→审核→发布→分发全流程                         │
├─────────────────────────────────────────────────────────────────────────────┤
│                                                                             │
│  ┌─────────┐     ┌─────────┐     ┌─────────┐     ┌─────────┐            │
│  │ 数据创建 │ ──► │ 数据校验 │ ──► │ 审核审批 │ ──► │ 正式发布 │            │
│  │         │     │         │     │         │     │         │            │
│  │ 录入信息 │     │ 完整性   │     │ 自动校验 │     │ 版本记录 │            │
│  │ 补充属性 │     │ 准确性   │     │ 人工复核 │     │ 索引构建 │            │
│  │ 上传证照 │     │ 规范性   │     │ 审批流   │     │ 权限更新 │            │
│  └────┬────┘     └────┬────┘     └────┬────┘     └────┬────┘            │
│       │               │               │               │                  │
│       │        ┌──────┴──────┐        │               │                  │
│       │        ▼              ▼        ▼               │                  │
│       │  ┌─────────┐   ┌─────────┐ ┌─────────┐        │                  │
│       │  │ 校验通过 │   │ 校验失败 │ │         │        │                  │
│       │  │         │   │         │ │         │        │                  │
│       │  │ 继续流程 │   │ 返回修改 │ │         │        │                  │
│       │  └─────────┘   └─────────┘ └─────────┘        │                  │
│       │                                                   │                  │
│       │              ┌─────────────────────┐             │                  │
│       │              ▼                     ▼             ▼                  │
│       │       ┌─────────┐           ┌─────────┐ ┌─────────────┐         │
│       │       │ 审核通过 │           │ 审核拒绝 │ │             │         │
│       │       │         │           │         │ │   正式发布   │         │
│       │       │         │           │ 返回修改 │ │             │         │
│       │       └────┬────┘           └─────────┘ │  • 版本号+1   │         │
│       │            │                            │  • 状态更新   │         │
│       │            │                            │  • 时间戳     │         │
│       │            │                            └──────┬──────┘         │
│       │            │                                   │                  │
│       │            │                                   ▼                  │
│       │            │                          ┌─────────────────┐         │
│       │            │                          │   数据分发      │         │
│       │            │                          │                 │         │
│       │            │                          │ • 事件发布      │         │
│       │            │                          │ • 消息推送      │         │
│       │            │                          │ • 订阅消费      │         │
│       │            │                          └────────┬────────┘         │
│       │            │                                   │                  │
│       │            │              ┌─────────────────────┘                  │
│       │            │              ▼                                          │
│       │            │       ┌─────────────┐                                  │
│       │            │       │  分发成功   │                                  │
│       │            │       │             │                                  │
│       │            │       │ 业务系统同步│                                  │
│       │            │       │ 更新本地缓存│                                  │
│       │            │       └─────────────┘                                  │
│       │            │                                                   │
│       └────────────┘                                                   │
│                                                                        │
└─────────────────────────────────────────────────────────────────────────────┘
```

### 9.2 用户注册→实名认证→身份绑定流程

```
用户注册 → 手机验证 → 基础信息录入 → 实名认证 → 身份选择 → 认证审核 → 身份绑定 → 完成
```

**详细说明**：
1. **用户注册**：输入手机号、验证码、设置密码
2. **手机验证**：发送短信验证码，验证手机有效性
3. **基础信息录入**：填写姓名、证件类型、证件号码等
4. **实名认证**：个人用户进行人脸识别+身份证验证，企业用户进行工商数据验证
5. **身份选择**：选择主体身份类型（农户、批发商、服务商等）
6. **认证审核**：提交认证资料，等待平台审核
7. **身份绑定**：审核通过后，绑定对应身份角色
8. **完成**：用户主数据创建完成，可使用平台功能

### 9.3 主数据变更分发与同步流程

```
变更触发 → 变更检测 → 变更封装 → 规则匹配 → 消息发布 → 订阅消费 → 同步确认
```

**详细说明**：
1. **变更触发**：用户修改主数据，触发变更事件
2. **变更检测**：系统检测数据变更，生成变更记录
3. **变更封装**：将变更数据封装为标准事件消息
4. **规则匹配**：根据分发规则匹配目标订阅模块
5. **消息发布**：通过消息队列发布变更事件
6. **订阅消费**：各业务模块消费消息，更新本地数据
7. **同步确认**：确认数据同步完成，记录同步状态

---

## 分阶段实施计划

### 10.1 MVP阶段（核心主数据建设）

**时间范围**：第1-3个月

**功能范围**：
- 用户主数据基础模型（用户CRUD、身份管理）
- 组织主数据基础模型（组织架构、部门管理）
- RBAC权限模型（角色管理、权限配置）
- 基础数据字典
- 行政区划基础数据

**目标指标**：
- 用户主数据API响应时间<200ms
- 支持1000并发用户访问
- 完成3个核心业务模块的数据对接

**里程碑**：
- M1：完成用户主数据基础功能
- M2：完成组织主数据和权限模型
- M3：完成数据字典和行政区划数据

### 10.2 V1.0阶段（主数据体系完善）

**时间范围**：第4-6个月

**功能范围**：
- 商品主数据体系（分类管理、标准库、审核流程）
- 客户供应商主数据
- 地址主数据（产区、物流节点）
- 数据编码规则
- 数据分发同步机制

**目标指标**：
- 主数据分发成功率>99%
- 支持5000并发用户访问
- 完成全部8个业务模块的数据对接

**里程碑**：
- M4：完成商品主数据体系
- M5：完成客户供应商和地址主数据
- M6：完成分发同步机制

### 10.3 V2.0阶段（数据治理能力增强）

**时间范围**：第7-9个月

**功能范围**：
- 数据标准管理（标准定义、审核发布、对标检测）
- 数据质量管理（规则引擎、检测任务、问题工单）
- 数据质量仪表盘
- 主数据治理运营工具

**目标指标**：
- 主数据完整率>98%
- 主数据准确率>99%
- 质量问题平均处理时间<24小时

**里程碑**：
- M7：完成数据标准管理
- M8：完成数据质量管理
- M9：完成治理运营工具

### 10.4 与各业务模块的实施节奏对齐

| 业务模块 | 依赖主数据 | 对接时间 | 说明 |
|:---------|:-----------|:---------|:-----|
| 生产管理 | 用户、组织、商品、地址 | V1.0 | 优先对接 |
| 交易撮合 | 用户、商品、客户供应商、地址 | V1.0 | 核心模块 |
| S2B2C | 用户、组织、商品、权限 | V1.0 | 同步对接 |
| 供应链金融 | 用户、组织、客户供应商 | V1.0 | 同步对接 |
| 信息资讯 | 用户、组织 | MVP | 轻量对接 |
| 综合服务 | 用户、组织、商品、地址 | V1.0 | 同步对接 |
| 政府监管 | 全部主数据 | V2.0 | 全面对接 |

---

## 附录

### 附录A：数据表清单汇总

> **说明**：所有业务表均包含 `tenant_id` 字段（系统表除外），实现行级租户隔离。以下标注"⭐"的表为业务表，标注"☆"的表为系统表（不需要tenant_id）。

| 序号 | 表名 | 说明 | 主键 | 隔离说明 |
|:-----|:-----|:-----|:-----|:---------|
| **租户管理表（系统表）** |
| 1 | tenant_info | ⭐ 租户信息表 | tenant_id | 系统表，无需隔离 |
| 2 | tenant_config | ⭐ 租户配置表 | config_id | 系统表，无需隔离 |
| **用户主数据** |
| 3 | user_master | ⭐ 用户主数据表 | user_id | tenant_id行级隔离 |
| 4 | user_identity | ⭐ 用户身份表 | identity_id | tenant_id行级隔离 |
| 5 | user_profile | ⭐ 用户画像表 | profile_id | tenant_id行级隔离 |
| 6 | user_authentication | ⭐ 用户认证表 | auth_id | tenant_id行级隔离 |
| **组织主数据** |
| 7 | organization | ⭐ 组织主数据表 | org_id | tenant_id行级隔离 |
| 8 | org_hierarchy | ⭐ 组织层级关系表 | hierarchy_id | tenant_id行级隔离 |
| 9 | org_certification | ⭐ 组织认证表 | cert_id | tenant_id行级隔离 |
| **权限主数据** |
| 10 | role | ⭐ 角色表 | role_id | 混合隔离 |
| 11 | permission | ☆ 权限表 | permission_id | 共享表 |
| 12 | role_permission | ⭐ 角色权限关联表 | id | tenant_id行级隔离 |
| 13 | user_role | ⭐ 用户角色关联表 | id | tenant_id行级隔离 |
| 14 | data_permission | ⭐ 数据权限表 | dp_id | tenant_id行级隔离 |
| **商品主数据** |
| 15 | product_category | ⭐ 商品分类表 | category_id | 混合隔离 |
| 16 | product_attribute_template | ⭐ 属性模板表 | template_id | tenant_id行级隔离 |
| 17 | product_master | ⭐ 商品主数据表 | product_id | tenant_id行级隔离 |
| 18 | product_quality_standard | ⭐ 质量标准表 | standard_id | 共享表 |
| **客户供应商主数据** |
| 19 | customer_master | ⭐ 客户主数据表 | customer_id | tenant_id行级隔离 |
| 20 | supplier_master | ⭐ 供应商主数据表 | supplier_id | tenant_id行级隔离 |
| 21 | customer_supplier_relation | ⭐ 客户供应商关系表 | relation_id | tenant_id行级隔离 |
| **地址主数据** |
| 22 | address_region | ☆ 行政区划表 | region_id | 共享表（国标） |
| 23 | product_region | ⭐ 产区表 | region_id | tenant_id行级隔离 |
| 24 | wholesale_market | ⭐ 批发市场表 | market_id | tenant_id行级隔离 |
| 25 | logistics_node | ⭐ 物流节点表 | node_id | tenant_id行级隔离 |
| **数据标准管理** |
| 26 | data_dictionary | ⭐ 数据字典表 | dict_id | 混合隔离 |
| 27 | data_code_rule | ☆ 编码规则表 | rule_id | 共享表 |
| 28 | data_format_standard | ☆ 格式标准表 | standard_id | 共享表 |
| **数据质量管理** |
| 29 | data_quality_rule | ⭐ 质量规则表 | rule_id | tenant_id行级隔离 |
| 30 | data_quality_result | ⭐ 质量检测结果表 | result_id | tenant_id行级隔离 |
| 31 | data_issue | ⭐ 数据问题表 | issue_id | tenant_id行级隔离 |

**租户信息表（tenant_info）结构**：

| 字段名 | 字段类型 | 长度 | 说明 | 示例 |
|:-------|:---------|:-----|:-----|:-----|
| tenant_id | VARCHAR | 32 | 租户ID，主键 | TEN20250100001 |
| tenant_code | VARCHAR | 50 | 租户编码 | AGRICULTURE_ENT_001 |
| tenant_name | VARCHAR | 200 | 租户名称 | 北京xx农业科技有限公司 |
| tenant_type | ENUM | - | 租户类型：platform/enterprise/personal | enterprise |
| contact_person | VARCHAR | 100 | 联系人 | 张三 |
| contact_phone | VARCHAR | 20 | 联系电话 | 13800138000 |
| contact_email | VARCHAR | 100 | 联系邮箱 | zhangsan@example.com |
| max_users | INT | - | 最大用户数 | 100 |
| max_storage | BIGINT | - | 最大存储空间(字节) | 10737418240 |
| max_api_calls | INT | - | 每日API调用次数 | 10000 |
| status | ENUM | - | 状态：pending/active/suspended/archived | active |
| expire_date | TIMESTAMP | - | 过期日期 | 2026-01-01 |
| create_time | TIMESTAMP | - | 创建时间 | 2025-01-01 |
| update_time | TIMESTAMP | - | 更新时间 | 2025-01-15 |

**租户配置表（tenant_config）结构**：

| 字段名 | 字段类型 | 长度 | 说明 | 示例 |
|:-------|:---------|:-----|:-----|:-----|
| config_id | VARCHAR | 32 | 配置ID，主键 | CFG20250100001 |
| tenant_id | VARCHAR | 32 | 租户ID | TEN20250100001 |
| config_key | VARCHAR | 100 | 配置键 | max_products |
| config_value | VARCHAR | 500 | 配置值 | 1000 |
| config_type | VARCHAR | 50 | 配置类型 | quota/feature/system |
| description | VARCHAR | 200 | 配置描述 | 最大商品数量 |
| status | ENUM | - | 状态 | enabled |
| create_time | TIMESTAMP | - | 创建时间 | 2025-01-01 |

### 附录B：API接口清单汇总

| 类别 | 接口数量 | 说明 |
|:-----|:---------|:-----|
| 租户管理 | 9 | 租户CRUD、配额、统计、穿透查询 |
| 用户主数据 | 12 | 用户CRUD、身份、认证、画像 |
| 组织主数据 | 11 | 组织CRUD、部门、岗位、认证 |
| 权限主数据 | 10 | 角色、权限、审计 |
| 商品主数据 | 10 | 分类、标准库、编码 |
| 客户供应商 | 10 | 客户、供应商、关系 |
| 地址主数据 | 8 | 行政区划、产区、物流 |
| 数据标准 | 6 | 字典、编码、格式 |
| 数据质量 | 9 | 规则、检测、工单、仪表盘 |
| 分发同步 | 6 | 订阅、分发、监控 |
| **合计** | **91** | - |

### 附录C：术语表

| 术语 | 英文 | 定义 |
|:-----|:-----|:-----|
| 主数据 | Master Data | 企业内跨越多个业务部门、被多个业务用户共同使用的核心业务数据 |
| 主数据管理 | MDM | 管理主数据的标准定义、创建、分发、治理的完整体系 |
| RBAC | Role-Based Access Control | 基于角色的访问控制 |
| SPU | Standard Product Unit | 标准化产品单元 |
| SKU | Stock Keeping Unit | 库存量单位 |
| CDC | Change Data Capture | 变更数据捕获 |
| MDG | Master Data Governance | 主数据治理 |
| 多租户 | Multi-Tenancy | 一种软件架构模式，多个租户共享同一套系统资源但数据隔离 |
| 租户 | Tenant | 使用平台服务的独立客户实体（企业或个人） |
| 行级隔离 | Row-Level Isolation | 通过tenant_id字段实现的数据隔离方式 |
| 配额 | Quota | 租户可使用的资源上限 |

---

**文档结束**
